# 闯关式 AI 编程学习平台 — 项目分析总结报告

> 项目 vs 参考代码 · Docker 分析 · 路线评估  
> **2026年7月**

---

## 目录

1. [项目与参考代码的区别与联系](#一项目与参考代码的区别与联系)
2. [Docker 深度分析](#二docker-深度分析)
3. [思路是否需要调整](#三思路是否需要调整)
4. [总结与建议](#四总结与建议)

---

## 一、项目与参考代码的区别与联系

### 1.1 联系 — 题材和底层思想一致

两个项目都是"**闯关式 AI 编程学习平台**"，后端都选 **FastAPI**，AI 都用 **DeepSeek API**，认证都用 **JWT + bcrypt**，都有用户系统、关卡系统、AI 导师三个核心模块。**业务概念层面没有走偏。**

### 1.2 区别 — 技术栈和规模完全不同

你们的方案不是参考代码的简化版，而是选择了另一条技术路线。参考代码是**成熟产品级实现**（v6.0），前端用 React 19 SPA 完整前后端分离，后端用异步 ORM 和 Alembic 迁移，支持 14 语言 258 个关卡，有 Docker 沙箱、智能体工坊、ML 实验室等大量功能。你们的方案是**轻量级 MVP**，用 HTMX 服务端渲染降低前端复杂度，聚焦 10 关 Python 基础，不执行用户代码。

| 维度 | 参考代码（CodeQuest） | 你们的方案 |
|------|---------------------|-----------|
| **前端技术** | React 19 + TypeScript + TailwindCSS 4 | HTMX + Alpine.js + Bootstrap 5 |
| **后端** | FastAPI + SQLAlchemy 2.0 async | FastAPI + pymysql 直接 SQL |
| **数据库** | SQLite (dev) / PostgreSQL (prod) | MySQL |
| **内容规模** | 14 语言 × 258 关卡 + 智能体工坊 + ML 实验室 | 10 关 Python 基础 |
| **代码执行** | Docker 沙箱执行用户代码 | **不执行**，仅文本提交给 AI 审查 |
| **编辑器** | Monaco Editor | CodeMirror |
| **管理后台** | React + Ant Design 6（仪表盘/用户/课程/成就/审计） | 无独立管理后台 |
| **部署方式** | Docker Compose 编排 | 本地 → ngrok → 云服务器（递进） |
| **特色功能** | 成就系统、排行榜、热力图、代码收藏、智能体工坊 | 审计日志、免费增值配额 |

### 1.3 分工和方向是否一致

**方向一致**（都是闯关学习平台），但**实现路径不同**。参考代码是单人/小团队长期迭代的产物，没有"4人分工"的概念。你们的分工（A用户系统 / B关卡编辑器 / C AI模块 / D内容设计）与参考代码的模块划分可以对应：

- 参考代码 `auth/user` 模块 → 你们的 **A 角色**
- 参考代码 `lessons/judge` 模块 → 你们的 **B 角色**
- 参考代码 `services/ai` 模块 → 你们的 **C 角色**
- 参考代码 `codequest-content` 目录 → 你们的 **D 角色**

**分工合理，可以继续执行。**

---

## 二、Docker 深度分析

### 2.1 参考代码为什么用 Docker

CodeQuest 用 Docker 做两件事：

1. **代码沙箱** — 安全执行用户提交的代码（no-network, 256MB 内存限制, 只读文件系统）
2. **ML 实验室** — 启动 JupyterLab + PyTorch 容器供学生做机器学习实验

这两件事都涉及**运行不可信代码**，必须隔离，所以 Docker 是刚需。

### 2.2 你的项目有必要用 Docker 吗？

**结论：现阶段没有必要。**

| 角度 | 分析 |
|------|------|
| **代码执行** | 设计为"不执行用户代码，仅提交文本给 AI 审查"，不需要沙箱。没有 Docker 沙箱的需求，这是最大的决定因素 |
| **服务数量** | 只有一个 FastAPI 后端 + MySQL 数据库，两个服务，手动启动即可，编排复杂度极低 |
| **团队能力** | 4 人都是初次做项目，3 周时间，学习 Docker 本身需要 2-3 天 |
| **开发环境** | 每人本地 MySQL 直连，不需要容器化 |

### 2.3 Docker 的局限和困难

- **学习成本高**：从零到能独立写 Dockerfile + docker-compose.yml，新手平均需要 2-3 天的连续学习时间
- **Windows 上的坑多**：Docker Desktop 资源占用大，WSL2 配置复杂，文件挂载权限问题、端口冲突排查对新手不友好
- **调试困难**：容器内出问题，新手很难定位（日志在哪？怎么进容器？数据丢了怎么办？）
- **对演示没有直接增益**：评委看的是功能演示，不是"你用了 Docker"

### 2.4 不用 Docker 的最终呈现

演示时：打开终端 → `uvicorn main:app --reload` → 浏览器打开 `http://localhost:8000` → 演示功能。**和使用 Docker 后的演示效果完全一样。** 评委不会在意你是用 `uvicorn` 启动的还是 `docker-compose up` 启动的。

如果上 Docker 反而有风险：演示时 Docker 容器起不来 → 花时间排查 → 演示翻车。

### 2.5 答辩中的增益是否值得

**不值得。** 同样 2-3 天时间，花在打磨功能、优化 UI、准备演示脚本上的收益远高于学习 Docker。

| 项目 | 不用 Docker | 用 Docker | 增益 |
|------|-----------|----------|:----:|
| 演示效果 | 本地运行，功能正常 | 本地运行，功能正常 | 无差异 |
| 代码安全性 | 不执行用户代码，无风险 | 不执行用户代码，无风险 | 无差异 |
| 评委印象 | 功能完整、逻辑清晰 | 功能完整 + 用了 Docker | 微 |
| 学习成本 | 0 | 2-3 天 | 负收益 |
| 翻车风险 | 低 | 中（Windows 兼容性问题） | 负收益 |

唯一可能加分的情况：如果评委问"你们用了什么部署技术？"你说"我们用了 Docker 容器化部署"，会有一个印象加分。但这个加分**远低于**把同样 2-3 天时间花在核心功能上的收益。

**建议：** 如果时间充裕（还剩 1 周以上且核心功能全部完成），可以花 1 天尝试在单台机器上用 Docker 部署。否则，**优先保证功能完整，不要碰 Docker。**

---

## 三、思路是否需要调整

### 3.1 思路不是"完全不同"，而是"合理简化"

参考代码是**用 React 做的完整 SPA**，你们是**用 HTMX 做的服务端渲染**。两者在业务逻辑层面一致，在技术实现层面走了不同但合理的路。这不是"完全不一样"，而是**在同一个目标下选择了更适合自己团队的技术路线**。

### 3.2 不需要调整的大方向

- ✅ **技术栈选型**（FastAPI + HTMX + Alpine.js）—— 合理，符合团队能力
- ✅ **4 人分工**（A/B/C/D）—— 合理，模块边界清晰
- ✅ **10 关 Python 基础的内容范围** —— 合理，3 周内可完成
- ✅ **不执行用户代码的设计** —— 合理，避免了安全风险
- ✅ **递进式部署策略**（本地→ngrok→云）—— 合理，务实
- ✅ **手动测试 + 录屏备份的演示方案** —— 合理，稳妥

### 3.3 建议微调的地方

#### ① 参考架构设计，不要照抄代码

- 参考代码的 `app/main.py` / `config.py` / `database.py` 的分层结构值得参考
- 参考代码的 `services/` 目录（把 AI 调用封装成独立模块）的设计值得参考
- 参考代码的 `routers/` 路由分离设计值得参考

#### ② 学习项目结构而非代码实现

参考代码的 `app/models/`、`app/schemas/`、`app/routers/`、`app/services/`、`app/core/` 的分层是好的工程实践。你们目前的项目结构比较简单（所有文件平铺在根目录），可以借鉴这个分层思路：

```
coding_game/                 # 你们的项目结构（建议演进方向）
├── main.py                  # FastAPI 应用入口
├── config.py                # 配置集中管理
├── database.py              # 数据库连接
├── models/                  # 数据模型
│   ├── user.py
│   └── level.py
├── routers/                 # 路由
│   ├── auth.py
│   ├── levels.py
│   └── ai.py
├── services/                # 业务逻辑
│   └── deepseek.py
├── templates/               # HTML 模板
└── static/                  # 静态文件
```

#### ③ 管理后台可简化，但不建议完全放弃

参考代码有独立的管理后台（React + Ant Design），你们目前没有。但至少需要一个简单的**审计日志查看页面**（已在 C 角色计划中）。如果时间允许，加一个简单的 `admin.html` 页面，用 HTMX 显示用户列表和日志，对答辩有加分。

#### ④ 借鉴"进度展示"概念

你们不需要做完整的成就系统，但可以在关卡列表页面显示**进度条/完成状态**。这已经在你们的设计中有了（`user_progress` 表 + 关卡列表页面），保持即可。

---

## 四、总结与建议

你们目前的思路是对的。参考代码是一个成熟产品（v6.0），你们在做的是一个 3 周 MVP。用更轻量的技术栈、更小的范围、更简单的架构，是**正确的工程决策**，不是"走偏了"。

如果担心"和参考代码不一样"会被导师批评，可以这样解释：

> 我们参考了 CodeQuest 的业务架构设计（用户/关卡/进度/AI 四大模块），但在技术实现上选择了更适合我们团队能力的 HTMX 路线。这样做的好处是：4 人 3 周内能交付完整可用的 MVP，且代码都是我们自己写的，没有照搬照抄。

### 最终建议清单

| 序号 | 建议 | 优先级 |
|:----:|------|:------:|
| 1 | 保持现有技术方案，不切换为 React SPA | **高** |
| 2 | 不使用 Docker，聚焦核心功能开发 | **高** |
| 3 | 参考 CodeQuest 的分层项目结构 | 中 |
| 4 | 审计日志页面保留，有余力加简单管理页面 | 中 |
| 5 | 本地演示 + OBS 录屏备份，不依赖 Docker 部署 | **高** |
| 6 | 代码全部自己写，不照搬参考代码 | **高** |
| 7 | 时间充裕时再考虑 Docker 部署 | 低 |

---

**把参考代码当作"架构参考"和"工程规范示范"，而不是"要模仿的代码"。专注写好你们自己的代码，保证 3 周后能跑通完整流程。**