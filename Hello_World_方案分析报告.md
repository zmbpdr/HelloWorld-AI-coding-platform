# Hello World 闯关式 AI 编程学习平台 — 方案分析报告

> 团队：4 人 | 周期：3 周 | 新手团队 | 基于参考代码二次开发 | 必须大改，不能完全照搬

---

## 一、背景与约束

### 1.1 团队情况

- 4 人团队，几乎都是第一次做这样的项目
- 新组建的团队，默契尚未建立，沟通需要慢慢磨合
- 3 周开发周期，时间紧张

### 1.2 核心约束

1. 必须在参考代码的基础上进行改造，**不能完全照搬照抄，必须进行修改，甚至是大改**
2. **不部署 Docker**，需要纯本地开发模式运行
3. 产品必须是**智能体形式**，AI 不能只是被动的聊天框
4. 项目命名为 **"Hello World"**，需要全新品牌形象
5. 四个人工作量要几乎均衡，合作部分尽量少且易于沟通

### 1.3 参考代码基础数据

经实际验证，参考代码的关卡内容文件完整：

| 语言 | 关卡数 | 语言 | 关卡数 |
|------|:------:|------|:------:|
| Python | 24 | Kotlin | 16 |
| JavaScript | 20 | PHP | 16 |
| Java | 20 | Lua | 14 |
| C++ | 18 | Shell | 14 |
| Go | 24 | Rust | 24 |
| TypeScript | 20 | SQL | 16 |
| Ruby | 16 | Swift | 16 |
| **合计** | | | **14 种语言，258 关** |

**但存在关键 Bug：** `seed_service.py` 中 `LANGUAGES_DATA` 只定义了 Python、JavaScript、Java、C++ 四种语言，其余 10 种语言虽然有 JSON 文件，但因为数据库中没有对应的 `Language` 记录，永远不会被导入。新建空数据库只能导入约 82 关，而非 258 关。这是一个必须修复的基础问题。

---

## 二、五个方案概述

### 方案一：精简版（技术栈收敛）

**核心思路：** 保留 FastAPI + React 技术栈骨架，但大幅精简功能范围。仅 Python 语言，50-80 关，合并前端学习端与管理后台为单端，移除 Docker 沙箱改用 subprocess 直接执行，AI 仅 DeepSeek 单后端，移除 Redis 缓存和 WebSocket。

**最终产品形态：** 一个标准的 Web 应用，后端 FastAPI 提供 API + 前端 React SPA 通过 Vite 构建。启动需要两个终端窗口（后端 uvicorn + 前端 vite dev server）。用户通过浏览器访问，完成 Python 编程闯关。

| 维度 | 内容 |
|------|------|
| 技术栈 | FastAPI + React + Vite + TailwindCSS + Monaco Editor + SQLite |
| 语言范围 | 仅 Python |
| 关卡数量 | 50-80 关 |
| 代码执行 | Python subprocess 直接执行 |
| 前端形态 | 双端合并为单端（学习+管理在一个 React 应用中） |
| 启动方式 | 两个终端：`uvicorn` + `vite` |
| 开发周期 | 3-6 周 |

---

### 方案二：Flask 极简版（单文件原型）

**核心思路：** 用最少的文件、最简的技术栈，快速验证核心闭环。后端 Flask 单文件 `app.py` 同时提供 API 和静态文件服务，前端纯 HTML + CSS + JavaScript 实现所有页面（通过 div 切换展示不同功能），数据库用 SQLite，代码执行用 Python subprocess，AI 调用 DeepSeek API。

**最终产品形态：** 一个极简的 Web 应用，整个项目仅 5-6 个文件，一条 `python app.py` 命令即可启动。前端没有构建工具，所有页面通过 div 的显示/隐藏切换。适合快速原型验证或教学演示。

| 维度 | 内容 |
|------|------|
| 技术栈 | Flask + 纯 HTML/CSS/JS + SQLite |
| 文件数 | 5-6 个 Python/HTML 文件 |
| 前端 | 无构建工具，纯 div 切换页面 |
| 启动方式 | 一条命令：`python app.py` |
| 开发周期 | 1-2 周 |

---

### 方案三：二次开发 + 三大 AI 创新

**核心思路：** 不重写，基于现有完整代码做"改造 + 创新"，把精力集中在三个 AI 创新点上。核心创新是 AI 出题引擎（AI 动态生成新题，而非静态 JSON）、代码审查可视化（逐行标注 + 雷达图）、自适应学习路径。预估改约 500 行 + 新增约 500 行代码。

**最终产品形态：** 一个 AI 驱动的编程学习平台。用户登录后进入闯关地图，遇到卡关时 AI 可以基于当前知识点自动生成变体题。提交代码后，AI 逐行标注代码问题，并用雷达图从五个维度（可读性/性能/简洁度/规范性/健壮性）展示评分。学习路径根据用户表现动态调整，不是固定顺序。

| 维度 | 内容 |
|------|------|
| 技术栈 | 复用原 FastAPI + React + Vite + TailwindCSS + Monaco Editor |
| 语言范围 | 仅 Python |
| 关卡内容 | 10-15 关静态关卡 + AI 动态生成无限关卡 |
| 核心创新 | AI 出题引擎、代码审查可视化（雷达图+逐行标注）、自适应学习路径 |
| 改动量 | 改约 500 行 + 新增约 500 行 |
| 启动方式 | 两个终端：`uvicorn` + `vite` |

---

### 方案四：AI 自适应编程学习（保留完整底座）

**核心思路：** 保留参考代码的完整架构和 258 个关卡，在此基础上改造成"AI 自适应编程学习平台"。核心创新是：从"固定顺序闯关"转变为"AI 根据用户能力水平动态推荐关卡"。新增能力诊断、知识点标签与掌握度画像、分层 AI 导师、AI 错题本、个性化推荐、学习周报等功能。同时修复参考代码中已知的 Bug。

**最终产品形态：** 一个功能完整的 AI 自适应编程学习平台。用户注册后先完成能力诊断测试（5-10 道题），系统生成能力画像并推荐合适的起点关卡。学习过程中，AI 导师提供三种模式（提示模式/讲解模式/代码审查模式），每次提交后自动更新知识点掌握度，错误代码自动记录到错题本。知识掌握度用可视化图表展示，学习周报自动生成。

| 维度 | 内容 |
|------|------|
| 技术栈 | 复用原 FastAPI + React + Vite + TailwindCSS + Monaco Editor + SQLite |
| 语言范围 | 14 种语言，258 关 |
| 核心创新 | 能力诊断、知识点掌握度、分层 AI 导师、AI 错题本、个性化推荐、学习周报 |
| 改动量 | 改约 500 行 + 新增约 800 行 |
| 启动方式 | 两个终端：`uvicorn` + `vite`，无 Docker |

---

### 方案五：Hello World 品牌重建（推陈出新）

**核心思路：** 在参考代码的基础上推陈出新，建立名为 "Hello World" 的全新品牌闯关式 AI 编程学习平台。不部署 Docker，不能完全照搬参考代码，必须进行修改甚至大改。核心诉求是：保留技术底座但重塑品牌和用户体验，做有差异化的产品。

**最终产品形态：** 一个名为 "Hello World" 的 AI 编程学习平台，品牌形象、UI 设计、关卡体系、功能创新都与参考代码有明显差异。无 Docker 依赖，纯本地开发模式运行。

| 维度 | 内容 |
|------|------|
| 技术栈 | 基于原 FastAPI + React，可结合方案四的改造内容 |
| 品牌 | 全新 "Hello World" 品牌 |
| 改动要求 | 必须大改，不能照搬 |
| 启动方式 | 两个终端，无 Docker |

---

## 三、五个方案科学性分析

### 3.1 科学性评估矩阵

| 评估维度 | 方案一 | 方案二 | 方案三 | 方案四 | 方案五 |
|----------|:---:|:---:|:---:|:---:|:---:|
| 充分利用已有代码 | 否（仍需重写大量代码） | 否（几乎全部重写） | 部分（利用架构，改核心逻辑） | **是（完整继承 258 关 + 全部架构）** | 是（从零重建项目结构） |
| 工作量估算精准度 | 偏乐观（3 周不够） | 偏乐观（演示效果差） | 适中 | **合理** | 适中 |
| 创新差异化清晰度 | 缺乏差异化 | 缺乏差异化 | **非常清晰**（AI 原生） | **非常清晰**（AI 自适应） | 需要再明确 |
| 对新手团队友好 | 中 | 差（单文件难协作） | **好**（模块独立） | **非常好**（模块边界清晰） | 中 |
| 风险可控性 | 中 | 高 | 中 | **低** | 中 |

### 3.2 方案一科学性分析

**优点：** 技术路线成熟，FastAPI + React 组合已被原项目验证过，去掉 Docker/Redis/WebSocket 后环境依赖极少。目录结构清晰，适合团队协作。

**问题：**
- 3-6 周的周期估算偏乐观。从零搭建 50-80 关的内容 + 完整前后端，实际需要 4-8 周。对于 4 个新手、3 周时间来说，完成度可能只有 50-60%
- 本质上是"重写"而非"改造"，没有充分利用已有 258 关内容和完整后端代码
- 合并前端和后台为单端意味着需要重新设计 UI 布局，反而增加工作量
- 没有提出任何创新点，只是做了一个更小的版本，评委眼中缺乏亮点

### 3.3 方案二科学性分析

**优点：** 启动极简，一条命令即可；学习曲线最低，不需要前端构建工具；最适合快速原型验证。

**问题：**
- **演示风险高**：纯 HTML 前端用 div 切换做"页面"，在评委面前展示显得粗糙，缺乏专业感。对于参赛场景来说，这是致命的
- **可扩展性差**：Flask 单文件模式下，要加创新功能代码会迅速膨胀为几千行的"大泥球"，后期维护困难
- **团队协作困难**：单文件模式下多人并行开发几乎不可能，Git 合并冲突会频繁发生。4 个人同时改一个 `app.py` 和一个 `index.html`，冲突将是灾难性的
- **Monaco 编辑器集成麻烦**：纯前端方案需要手动加载 CDN，体验和加载速度都远不如 Vite 构建方案
- **CDN 依赖风险**：如果现场网络不好，CDN 资源加载失败，页面直接无法使用
- **与创新方向冲突**：雷达图、代码逐行标注等可视化功能在纯 HTML 中实现难度远高于 React 组件化

### 3.4 方案三科学性分析

**优点：**
- 工作量最可控：改 500 行 + 新增 500 行，远少于从零写 2000 行
- 创新点最突出：AI 出题引擎是"质变"——别人是静态 JSON，你们是 AI 动态生成
- 演示效果最好：代码逐行标注 + 雷达图，评委一看就懂
- 团队协作顺畅：三个创新方向互不依赖，可以并行开发

**问题：**
- AI 出题引擎的 Prompt 质量是关键：如果 DeepSeek 生成的题目测试用例不可靠，演示时会出现"AI 出的题本身就有 bug"的尴尬情况
- AI 生成的测试用例可能不可靠，需要额外的校验机制
- 需要在前端做复杂的可视化组件（雷达图、代码高亮标注），有一定前端能力要求

### 3.5 方案四科学性分析

**优点：**
- **投入产出比最高**：在已有完整代码基础上做增量开发，不需要重写任何现有功能
- **风险最低**：即使创新功能不完美，基础功能（258 关闯关 + 评测 + AI 对话）已经能够完整演示。核心功能是可靠的基础，创新功能是增量
- **创新定位清晰**：从"固定顺序"到"AI 自适应"，不是简单的功能叠加，而是学习模式的根本改变
- **指出了参考代码的真实问题**：
  - 14 种语言只初始化了 4 种（`seed_service.py` 中 `LANGUAGES_DATA` 只定义了 4 种语言，其余 10 种永远不会导入）
  - AI 服务实际只调用 Ollama 本地模型 `qwen2.5:7b`，没有连接 DeepSeek
  - 管理员密码硬编码在代码中（`seed_service.py` 第 91 行 `admin/admin123`）
- **演示效果完整**：能力诊断 → 个性化路线 → 闯关 → AI 审查 → 错题本 → 知识画像 → 周报，整个流程完整闭环，评委能看到一个完整的学习循环

**问题：**
- 需要先花 1-2 天熟悉参考代码结构
- 知识点标签需要手动添加（但只针对 Python 的 24 关即可，D 一个人 1.5 天可完成）
- 推荐算法需要斟酌设计

### 3.6 方案五科学性分析

方案五本质上是"品牌约束"而非独立的技术方案，可以与其他方案结合。它的核心要求是：
- 项目名称改为 "Hello World"
- 不能照搬，必须大改
- 不部署 Docker

这些要求完全可以与方案四结合：在方案四的 AI 自适应架构基础上，将项目重命名为 "Hello World"，重新设计 UI 品牌形象，从深色科技风改为清新蓝白风格。

---

## 四、五个方案可行性评估

### 4.1 可行性综合评分

| 方案 | 科学性 | 可行性 | 风险等级 | 3 周完成度 | 演示效果 | 推荐指数 |
|------|:-------:|:-------:|:---------:|:-----------:|:---------:|:---------:|
| 方案一 | ★★★ | ★★★ | 中 | 50-60% | 中规中矩 | ★★★ |
| 方案二 | ★★ | ★★ | 高 | 80% | 简陋 | ★☆ |
| 方案三 | ★★★★ | ★★★★ | 低 | 85% | 有亮点 | ★★★★ |
| 方案四 | ★★★★★ | ★★★★★ | 最低 | **90%+** | **完整震撼** | **★★★★★** |
| 方案五 | - | - | - | - | - | 与方案四结合 |

### 4.2 方案一可行性：3 周不够

即便 4 人并行，方案一仍需要从零搭建大量内容：
- 需要写 50-80 个关卡的完整内容（题目、描述、入门代码、参考答案、测试用例），内容创作本身就是最耗时的工作
- 需要重新设计合并后的前端 UI（学习端和管理端合并）
- 需要重新组织 API 路由
- 实际工作量远超 3 周容量

### 4.3 方案二可行性：不适合 4 人团队

方案二的核心问题是"单文件"模式完全不支持 4 人并行开发。如果 4 个人同时修改一个 `app.py` 和一个 `index.html`，Git 冲突将是灾难性的。此外，纯 HTML 前端在演示时缺乏专业感，不适合参赛场景。

### 4.4 方案三可行性：需要注意 AI 出题质量

方案三的核心风险在于 AI 出题质量。如果 DeepSeek 生成的题目测试用例不可靠，演示时会出现"AI 出的题本身就有 bug"的尴尬情况。建议：
- 提前准备 3-5 道 AI 生成的题作为演示 demo
- 手动验证 AI 生成的测试用例是否正确
- 设计降级方案：如果 AI 出题失败，展示"AI 正在思考中"的友好提示

### 4.5 方案四可行性：最优解

方案四的可行性最高，原因：
- 已有 258 关是人工编写、测试通过的，质量有保证
- AI 只做推荐和审查，不直接生成题目，风险极低
- 增量开发，每个功能都是独立的，可以分头推进
- 即使某个功能没做完，基础功能完整可用

### 4.6 方案五可行性：与方案四完美结合

"Hello World" 品牌 + 方案四的 AI 自适应架构，既有品牌重塑，又有技术创新，是综合最优解。

---

## 五、最终推荐方案：方案四 + 方案五（AI 学习智能体 + AI 自适应）

### 5.1 产品形式

**产品形式是纯 Web 应用，不是静态网页。** Web 应用与静态网页的核心区别在于：Web 应用有后端、有数据库、有用户认证、每个人看到的内容不同（自己的学习数据），而静态网页所有人看到的内容一样。

**Hello World 是一个 Web 应用：** 用户打开浏览器访问 `http://localhost:5173`，看到的是 React 渲染的页面，但背后有 FastAPI 后端在处理请求、SQLite 数据库在存储每个用户的学习数据。用户注册登录后，看到的是自己的学习进度、自己的错题本、自己的知识画像——这正是 Web 应用的典型特征。

**为什么必须用 Web 应用而不是静态网页？** 因为核心功能——用户注册登录、代码提交评测、学习进度记录、AI 智能体对话、排行榜——这些都需要后端处理、数据库存储、每个用户看到自己的数据。这不是一个"展示型"的网页，而是一个"工具型"的 Web 应用。

### 5.2 AI 学习智能体"小智"

**智能体形式是根本要求。** 参考代码中，AI 是一个被动的聊天框——用户不主动提问，AI 就永远不说话。改造后，AI 是一个有名字、有性格、有记忆、能主动陪伴用户学习的智能体"小智"。它不是"加了个功能"，而是**改变了整个产品的交互形态**。

**智能体"小智"的四种模式：**

| 模式 | 何时触发 | 智能体扮演什么角色 | 具体行为 |
|------|----------|-------------------|----------|
| **诊断模式** | 注册后首次使用 | 面试官 | 出 5-10 道题测试用户水平，判断强弱项，主动打招呼："你好，我是小智，你的 AI 学习伙伴。我们先做个小测试，让我了解一下你的水平吧？" |
| **导师模式** | 学习过程中卡住时 | 老师 | 在侧边栏常驻，用户长时间未提交时主动出现："你卡住了吗？需要我提示一下吗？"。只给渐进式提示，不给完整答案 |
| **审查模式** | 提交代码后 | 考官 | 自动弹出审查面板，从正确性/可读性/效率/规范性四维度打分，雷达图展示，逐行标注问题："这行有问题，循环边界少了 1" |
| **规划模式** | 日常/阶段性 | 学习顾问 | 首页展示推荐："根据诊断，我建议从第 5 关开始"。第二天登录："欢迎回来！昨天你学了循环，今天继续吗？"。周末生成学习周报 |

**智能体"小智"与参考代码 AI 的本质区别：**

| 对比维度 | 参考代码的 AI | 智能体"小智" |
|----------|--------------|-------------|
| 主动性 | 被动：用户不问就不说话 | **主动**：用户卡住时主动出现，注册时主动打招呼 |
| 记忆 | 无：每次对话独立 | **有记忆**：知道用户学到哪里、薄弱点是什么 |
| 角色 | 单一聊天框 | **四种角色**：面试官、老师、考官、学习顾问 |
| 输出形式 | 纯文本 | **结构化 JSON**：评分 + 问题列表 + 逐行标注 |
| 连续性 | 无 | **贯穿全程**：从注册到周报，始终陪伴 |

### 5.3 智能体模型选型：DeepSeek-V4-Flash

**模型选择：DeepSeek-V4-Flash。** 该模型于 2026 年 4 月 24 日发布，是 DeepSeek 的轻量化旗舰模型，采用 MoE 架构（总参数 284B，每次激活约 13B），支持 100 万 token 上下文，MIT 开源许可。

**与旧方案（deepseek-chat）的对比：**

| 对比项 | deepseek-chat | DeepSeek-V4-Flash | 结论 |
|--------|:---:|:---:|:---:|
| 速度 | 标准 | **更快** | Flash 胜 |
| 价格 | 标准 | **更便宜** | Flash 胜 |
| JSON 输出能力 | 稳定 | 稳定 | 平手 |
| 中文能力 | 优秀 | 优秀 | 平手 |
| 编程理解能力 | 优秀 | 优秀 | 平手 |
| 上下文长度 | 标准 | 100 万 token | Flash 胜 |
| API 调用方式 | 标准 API | 完全兼容 | 平手 |

**对项目的具体适配：**

| 功能需求 | DeepSeek-V4-Flash 适配性 |
|----------|--------------------------|
| 智能体四种模式（诊断/导师/审查/规划） | 非思考模式速度快，适合对话场景 |
| 结构化代码审查（返回 JSON 格式评分） | JSON 格式输出稳定，适合解析 |
| 错题分析（自动分类错误类型） | 编程理解能力强 |
| 学习周报（自然语言总结） | 100 万上下文，能处理整个周的学习记录 |
| 成本控制 | 比标准版更便宜，学生团队无压力 |
| 免费途径 | 商汤 SenseNova 平台可免费调用（每 5 小时刷新额度） |

**改造方式：** `config.py` 中已有 `DEEPSEEK_API_KEY` 字段（第 30 行），新增 `DEEPSEEK_MODEL = "deepseek-v4-flash"` 配置项。`ai_service.py` 从硬编码 Ollama 本地模型 `qwen2.5:7b` 改为 `AIProvider` 抽象工厂模式，调用优先级：DeepSeek-V4-Flash → Ollama 降级 → Mock 兜底。

### 5.4 最终产品形态

**产品名称：** Hello World — AI 智能体陪你闯关编程学习

**完整用户流程：**

```
注册/登录
    ↓
AI 智能体"小智"主动打招呼："你好，我是小智，你的 AI 学习伙伴。我们先做个小测试，让我了解一下你的水平吧？"
    ↓
完成 5-10 道能力诊断题
    ↓
小智生成能力画像："你已经掌握了变量和条件判断，但循环还需要加强。我建议从第 5 关开始，跳过你已经掌握的基础知识。你同意吗？"
    ↓
同意推荐 → 进入对应关卡
不同意 → 自己选
    ↓
进入闯关 → 编写代码（Monaco 编辑器）
    ↓
提交代码
    ↓
├─ 通过 → 获得星级评分 + 经验值 → 更新知识点掌握度
│    ↓
│    小智："恭喜通关！循环这个知识点你已经掌握了。要不要趁热打铁，挑战下一关？"
│
└─ 失败 → 智能体自动审查：从正确性/可读性/效率/规范性四维度评分 → 雷达图 + 逐行标注问题
        ↓
    错误自动记录到错题本（分类：语法错误/逻辑错误/边界错误/性能问题）
        ↓
    小智："这行有问题，循环边界少了 1。我建议你..."
        ↓
    修改代码重新提交
    ↓
更新知识掌握度 → 根据薄弱知识点推荐下一题
    ↓
打开个人中心 → 查看知识掌握度画像（可视化图表）+ 错题本 + 本周学习报告
```

### 5.5 语言和关卡

| 语言 | 关卡数 | 知识点标签 | AI 智能体自适应 | 说明 |
|------|--------|:----------:|:---------------:|------|
| Python | 24 | ✅ 全部打标签 | ✅ 完整支持 | **核心创新区**，大改 |
| JavaScript | 20 | ❌ | ❌ 固定顺序 | 照搬 |
| Java | 20 | ❌ | ❌ 固定顺序 | 照搬 |
| C++ | 18 | ❌ | ❌ 固定顺序 | 照搬 |
| Go | 24 | ❌ | ❌ 固定顺序 | 照搬 |
| Rust | 24 | ❌ | ❌ 固定顺序 | 照搬 |
| TypeScript | 20 | ❌ | ❌ 固定顺序 | 照搬 |
| SQL | 16 | ❌ | ❌ 固定顺序 | 照搬 |
| Ruby | 16 | ❌ | ❌ 固定顺序 | 照搬 |
| Swift | 16 | ❌ | ❌ 固定顺序 | 照搬 |
| Kotlin | 16 | ❌ | ❌ 固定顺序 | 照搬 |
| PHP | 16 | ❌ | ❌ 固定顺序 | 照搬 |
| Shell | 14 | ❌ | ❌ 固定顺序 | 照搬 |
| Lua | 14 | ❌ | ❌ 固定顺序 | 照搬 |
| **合计** | **258** | **Python 24 关** | **Python 24 关** | |

**说明：** 创新聚焦于 Python 编程闯关。只给 Python 的 24 关手动打标签，D 一个人可以在 1.5 天内完成。其他 13 种语言保留但不做标记，作为平台内容丰富度展示。演示时主推 Python 的 AI 智能体自适应体验，其他语言作为"平台支持多种语言"展示。

### 5.6 智能体工坊

参考代码中已包含 8 条 AI/ML 主线（机器学习、深度学习、Agent 开发、大模型应用、自然语言处理、计算机视觉、强化学习、综合项目），共 92 个节点。**保留但不改造**——它已经在参考代码中完整可用，保留它可以增加平台内容丰富度，展示时可以说"我们还有 AI/ML 学习板块"。智能体"小智"也会在合适的时候推荐这个板块的学习。

---

## 六、改造方案详解

### 6.1 完全不改的（照搬，10 个模块）

| 模块 | 说明 |
|------|------|
| 用户注册/登录 | JWT 认证，直接使用 |
| 代码编辑器 | Monaco Editor，直接使用 |
| 排行榜 | 总榜/月榜/周榜，直接使用 |
| 成就系统 | 4 级稀有度，直接使用 |
| 学习热力图 | 直接使用 |
| 代码收藏 | 直接使用 |
| 14 种语言关卡 JSON | 题目、测试用例、参考答案，全部直接使用 |
| 智能体工坊 | 8 主线 92 节点，直接使用 |
| 代码评测基础逻辑 | 编译、运行、比对测试用例，直接使用 |
| 管理后台基础框架 | 用户管理、课程管理、仪表盘等，直接使用 |

---

### 6.2 小改动的（Bug 修复 + 必要调整，4 项）

| 序号 | 改动项 | 改什么 | 为什么 |
|:----:|--------|--------|--------|
| 1 | 补全 14 种语言初始化 | `seed_service.py` 的 `LANGUAGES_DATA`，从 4 种补到 14 种 | 不补的话，另外 10 种语言的 176 关永远用不了 |
| 2 | 管理员密码改为读 `.env` | `seed_service.py` 第 91 行，从 `.env` 读 `ADMIN_USERNAME` 和 `ADMIN_PASSWORD` | 原代码硬编码 `admin/admin123`，安全风险 |
| 3 | 代码执行去掉 Docker | `.env` 设 `ALLOW_DIRECT_EXECUTION=true`，`judge_service.py` 走 `_execute_direct()` | 无需 Docker 即可执行用户代码 |
| 4 | 管理后台新增标签管理 | `LessonManager.tsx` 增加知识点标签编辑入口 | 支持 Python 关卡打标签 |

---

### 6.3 大改动的（核心创新，聚焦 Python 闯关）

#### 6.3.1 AI 智能体"小智"（重写 `ai_service.py`）

**现状：** `ai_service.py` 硬编码调用 Ollama 本地模型 `qwen2.5:7b`，单一聊天框，System Prompt 固定，没有任何模式切换。

**改为：** `AIProvider` 抽象工厂模式。

```
调用优先级：DeepSeek-V4-Flash → Ollama 降级 → Mock 兜底
```

**AIProvider 抽象基类设计：**

```python
class AIProvider(ABC):
    @abstractmethod
    async def chat(self, messages: list[dict], mode: str) -> str: ...
    @abstractmethod
    async def chat_stream(self, messages: list[dict], mode: str) -> AsyncGenerator[str, None]: ...

class DeepSeekProvider(AIProvider):   # 调用 DeepSeek-V4-Flash API
class OllamaProvider(AIProvider):     # 降级备选，调用本地 Ollama
class MockProvider(AIProvider):       # 最终兜底，返回预设友好提示
```

**四种模式 System Prompt 设计：**

| 模式 | System Prompt 核心要点 |
|------|----------------------|
| 诊断模式 | "你是 AI 学习智能体小智。你正在给用户做能力诊断，根据用户回答判断掌握程度。请用友好的语气给出反馈，最后输出 JSON 格式的能力画像：{score, skill_level, correct_tags, weak_tags, recommended_start}" |
| 导师模式 | "你是编程导师小智。用户正在做一道题，你只能给渐进式提示，绝对不能给完整答案。最多给 3 条提示，每次只给一条。当用户长时间未提交时，主动询问是否需要帮助。" |
| 审查模式 | "你是代码审查官小智。请从正确性、可读性、效率、规范性四个维度评分（0-100），返回 JSON 格式：{correctness, readability, efficiency, compliance, problems: [{line, severity, message}], summary, next_action}" |
| 规划模式 | "你是学习顾问小智。根据用户的知识掌握度、错题记录和学习历史，推荐最适合的下一关，并生成自然语言的学习建议。" |

**涉及文件：**
- 后端：`ai_service.py`（重写），`config.py`（新增 `DEEPSEEK_MODEL` 配置项）
- 前端：`AIChat.tsx`（改造，增加模式切换按钮和智能体常驻侧边栏），`index.css`（智能体 UI 样式）

---

#### 6.3.2 能力诊断

**功能效果：** 用户注册后，智能体"小智"主动引导完成 5-10 道选择题，涵盖变量、条件判断、循环、函数等基础知识。根据正确率判断能力等级，推荐合适的起点关卡。

**后端新增：**

数据库新增 `UserDiagnostic` 表：
```
UserDiagnostic:
  user_id           -- 用户 ID
  score             -- 正确率 (0-100)
  skill_level       -- 能力等级 (beginner/intermediate/advanced)
  correct_tags      -- 掌握的知识点 (JSON)
  weak_tags         -- 薄弱知识点 (JSON)
  recommended_start -- 推荐起始关卡 slug
  created_at        -- 诊断时间
```

规则判断逻辑：
- 正确率 0%-30% → 从基础关开始（第 1 关）
- 正确率 31%-60% → 跳过 Hello World 等简单关卡
- 正确率 61%-80% → 从中级关卡开始
- 正确率 81%以上 → 推荐综合项目和其他语言

新增文件：`app/models/diagnostic.py`、`app/services/diagnostic_service.py`、`app/routers/diagnostic.py`

**前端新增：** `pages/Diagnostic.tsx`（注册后自动跳转），`App.tsx`（注册路由）

**工作量：** 约 2 天

---

#### 6.3.3 知识点掌握度

**功能效果：** 为 Python 24 关每关增加知识点标签，每次提交后根据结果更新对应知识点的掌握度。前端个人中心用可视化图表展示掌握度画像。

**后端新增：**

数据库新增 `UserKnowledge` 表：
```
UserKnowledge:
  user_id        -- 用户 ID
  knowledge_tag  -- 知识点标签（如 "循环"、"函数"）
  mastery        -- 掌握度 (0-100)
  total_attempts -- 总尝试次数
  last_practice  -- 最近练习时间
```

掌握度计算逻辑：
- 用户首次做某知识点相关的题 → 初始掌握度取决于正确率
- 后续做同类题通过 → 掌握度提升
- 后续做同类题失败 → 掌握度下降
- 加权平均计算每个知识点的最终掌握度

新增文件：`app/models/knowledge.py`、`app/services/knowledge_service.py`。修改 `app/routers/progress.py`（增加知识掌握度接口）。

**数据改造：** `python.json` 每关增加字段：
```json
{
  "slug": "python-01-hello-world",
  "knowledge_tags": ["print", "字符串"],
  "estimated_minutes": 15,
  "prerequisites": []
}
```

**前端改造：** `pages/Profile.tsx` 增加知识画像区域，`components/KnowledgeChart.tsx`（新建，条形图/热力图）

**工作量：** 约 2 天

---

#### 6.3.4 结构化代码审查

**功能效果：** 提交代码后，智能体从四个维度打分（正确性/可读性/效率/规范性），用雷达图展示，并在代码编辑器中逐行标注问题。

**后端新增：** `ai_service.py` 新增 `structured_review()` 方法，设计审查 Prompt 让 DeepSeek-V4-Flash 返回结构化 JSON：

```json
{
  "correctness": 85,
  "readability": 70,
  "efficiency": 65,
  "compliance": 80,
  "problems": [
    {"line": 5, "severity": "warning", "message": "变量名 a 不够描述性，建议改为 user_count"},
    {"line": 12, "severity": "error", "message": "循环边界可能错误，建议使用 len()-1"}
  ],
  "summary": "整体代码逻辑正确，但可读性和效率有提升空间。",
  "next_action": "建议重新练习列表遍历"
}
```

**前端新增：** `components/CodeReviewPanel.tsx`（新建，雷达图使用 Chart.js 实现 + 代码行高亮标注），`pages/Lesson.tsx`（修改，集成审查面板）

**降级方案：** 如果 AI 返回的 JSON 解析失败，降级为普通文本展示，隐藏雷达图，不影响正常使用。

**工作量：** 约 2.5 天

---

#### 6.3.5 AI 错题本

**功能效果：** 用户每次提交失败，错误代码自动记录到错题本，按错误类型分类（语法错误/逻辑错误/边界错误/性能问题），支持查看历史错误和 AI 分析，追踪是否已解决。

**后端新增：**

数据库新增 `UserError` 表：
```
UserError:
  user_id      -- 用户 ID
  lesson_id    -- 对应关卡
  error_code   -- 错误代码
  error_type   -- 错误类型 (syntax/logic/boundary/performance)
  ai_analysis  -- AI 分析结果
  fixed_code   -- 修改后的正确代码（如果用户后来通过了）
  is_resolved  -- 是否已解决
  created_at   -- 记录时间
```

错误分类逻辑：
- 代码执行返回 syntax error → 语法错误
- 代码执行成功但测试用例不通过 → 逻辑错误
- 测试用例边界值不通过 → 边界错误
- 代码执行超时 → 性能问题

新增文件：`app/models/error.py`、`app/services/error_service.py`、`app/routers/errors.py`

**前端新增：** `pages/ErrorBook.tsx`（新建，按错误类型分类展示，列表 + 筛选 + 展开查看详情），`App.tsx`（注册路由）

**工作量：** 约 2 天

---

#### 6.3.6 个性化推荐

**功能效果：** 智能体根据用户的知识掌握度和错题记录，推荐最适合的下一关。闯关地图和关卡页面展示推荐标记，不再固定顺序。

**后端新增：**

推荐算法（规则驱动，不需要机器学习）：
1. 获取用户薄弱知识点列表（掌握度最低的 3 个）
2. 查找覆盖这些薄弱知识点的关卡，按难度排序
3. 如果所有薄弱知识点已覆盖，推荐当前语言的下一个未完成关卡
4. 如果当前语言已完成，推荐其他语言的基础关卡

新增文件：`app/services/recommend_service.py`。修改 `app/routers/lessons.py`（增加推荐接口）。

**前端改造：** `pages/CourseMap.tsx`（修改，展示推荐标记），`pages/Lesson.tsx`（修改，展示"推荐下一关"按钮）

**降级方案：** 如果推荐算法不准，回退为"按顺序推荐下一关"（简单但可靠）。

**工作量：** 约 1.5 天

---

#### 6.3.7 学习周报（P2，有余力再做）

**功能效果：** 根据本周提交数据，统计完成关卡数、学习时长、常见错误、掌握最好的知识点、薄弱知识点，让 DeepSeek-V4-Flash 生成自然语言总结和下周推荐路线。

**后端新增：** 统计服务 + AI 总结生成接口

**前端新增：** 周报页面

**工作量：** 约 2 天

---

#### 6.3.8 免费版/Pro 版会员模拟（P2，有余力再做）

**功能效果：** 不接真实支付，只实现会员字段、配额判断和模拟升级。免费版每日 AI 对话限制 20 次，Pro 版不限。

**工作量：** 约 1 天

---

### 6.4 大改范围总结

| 文件 | 改动类型 | 说明 |
|------|:--------:|------|
| `ai_service.py` | **重写** | 抽象工厂 + 四种模式 + DeepSeek-V4-Flash 优先 |
| `seed_service.py` | 修改 | 补全 14 种语言 |
| `judge_service.py` | 修改 | 去掉 Docker 沙箱逻辑 |
| `config.py` | 修改 | 增加 `DEEPSEEK_MODEL`、`AI_PROVIDER_PRIORITY` 配置 |
| `.env` | 修改 | 增加 `ALLOW_DIRECT_EXECUTION`、`ADMIN_USERNAME`、`ADMIN_PASSWORD` |
| `python.json` | 修改 | 每关增加 `knowledge_tags`、`estimated_minutes`、`prerequisites` |
| `models/` | 新增 3 个文件 | `diagnostic.py`、`knowledge.py`、`error.py` |
| `services/` | 新增 4 个文件 | `diagnostic_service.py`、`knowledge_service.py`、`error_service.py`、`recommend_service.py` |
| `routers/` | 新增 3 个文件 | `diagnostic.py`、`errors.py`，推荐接口加到 `lessons.py` |
| 前端 `pages/` | 新增 2 个页面 | `Diagnostic.tsx`、`ErrorBook.tsx` |
| 前端 `pages/` | 改造 4 个页面 | `Lobby.tsx`、`CourseMap.tsx`、`Lesson.tsx`、`Profile.tsx` |
| 前端 `components/` | 新增 2 个组件 | `CodeReviewPanel.tsx`、`KnowledgeChart.tsx` |
| 前端 `components/` | 改造 1 个组件 | `AIChat.tsx` |
| 前端 `index.css` | 改造 | 配色方案改为 Hello World 清新蓝白风 |
| 管理后台 `LessonManager.tsx` | 改造 | 增加标签管理功能 |

---

## 七、四人分工方案

### 7.1 分工原则

- **每个人工作量几乎相等**（每人约 8-9 天）
- **模块独立，尽可能少合作**（适合新团队磨合）
- **合作部分有详细说明，每个人都能看懂**
- **责任清晰，每个人对自己负责的模块全权负责**

---

### 7.2 成员 A：平台基础设施 + 数据库 + 用户系统 + 会员体系

**角色定位：** 平台底座负责人，确保整个项目能跑、能部署、能演示。

**工作内容：**

| 阶段 | 任务 | 工作量 |
|------|------|--------|
| P0 | 全部 6 项修复工作（语言补全、AI 抽象层、管理员密码、检查脚本、移除 Docker、项目重命名） | 1 天 |
| P1 | 数据库迁移：新增 `UserDiagnostic`、`UserKnowledge`、`UserError` 三张表，编写数据模型 | 1 天 |
| P1 | 能力诊断后端逻辑：规则判断正确率 → 推荐起点，编写诊断 API 路由 | 1.5 天 |
| P1 | 用户系统改造：增加会员等级字段（free/pro），编写会员配额中间件 | 1.5 天 |
| P2 | 免费版/Pro 版会员模拟（有余力） | 1 天 |
| 收尾 | 部署文档、启动脚本、环境配置整理 | 1 天 |
| 收尾 | 项目整体联调（与所有人配合） | 1 天 |

**交付物：**
- 可运行的完整后端（所有路由正常）
- 数据库迁移脚本
- 内容检查脚本
- 启动说明文档
- 环境配置模板

**独立完成度：** 90% 独立。仅在接口定义时和其他人沟通。

---

### 7.3 成员 B：AI 智能体负责人（核心创新）

**角色定位：** AI 大脑开发者，负责所有 AI 相关的功能。

**工作内容：**

| 阶段 | 任务 | 工作量 |
|------|------|--------|
| P0 | AI 抽象层改造：`AIProvider` 接口 + `DeepSeekProvider`(DeepSeek-V4-Flash) + `OllamaProvider` + `MockProvider`，优先级策略 | 1 天 |
| P1 | 分层 AI 智能体：四种模式（诊断/导师/审查/规划）的 System Prompt 设计 + 调用接口 | 1.5 天 |
| P1 | 结构化代码审查：设计审查 Prompt 让 DeepSeek-V4-Flash 返回 JSON（四个维度评分 + 问题列表），解析和验证 JSON 格式 | 2 天 |
| P1 | AI 错题分析：自动判断错误类型（语法/逻辑/边界/性能），设计分类 Prompt | 1.5 天 |
| P2 | 学习周报 AI 总结生成（有余力） | 1 天 |
| 收尾 | Prompt 调优 + 测试 + 与前端联调 | 1 天 |

**交付物：**
- 可切换的 AI 提供器（DeepSeek-V4-Flash / Ollama / Mock）
- 四种模式接口（诊断/导师/审查/规划）
- 结构化评分接口（返回 JSON）
- 错误分类接口

**独立完成度：** 95% 独立。需要 A 建好表就能开始，只需要和 C 约定 AI 返回数据的格式。

---

### 7.4 成员 C：前端页面负责人 + 可视化

**角色定位：** 用户界面设计师，负责所有前端页面的改造和新增。

**工作内容：**

| 阶段 | 任务 | 工作量 |
|------|------|--------|
| P0 | 项目重命名：修改所有标题、品牌名称、配色方案为 "Hello World" | 0.5 天 |
| P1 | 新增能力诊断页面：选择题 UI + 提交 → 展示诊断结果和推荐起点 | 1.5 天 |
| P1 | 改造 Lesson 页面：AI 智能体四种模式切换按钮 + 切换 UI | 1 天 |
| P1 | 代码审查面板：雷达图组件（Chart.js 实现）+ 代码行高亮标注组件 | 2 天 |
| P1 | 新增错题本页面：按错误类型分类展示（列表 + 筛选 + 展开查看详情） | 1.5 天 |
| P1 | 个人中心改造：知识掌握度展示（条形图/热力图） | 1 天 |
| 收尾 | UI 打磨 + 与 B 联调 AI 数据展示 + 与 D 联调推荐展示 | 1 天 |

**交付物：**
- 全新 Hello World 品牌 UI
- 能力诊断页面
- 代码审查雷达图组件
- 错题本页面
- 知识掌握度可视化展示

**独立完成度：** 85% 独立。需要和其他人约定 API 返回数据格式，但这是第一天就定好的，不需要频繁沟通。

---

### 7.5 成员 D：内容 + 推荐算法 + 管理后台 + 测试

**角色定位：** 内容和质量负责人，负责关卡数据、推荐逻辑、管理后台和整体测试。

**工作内容：**

| 阶段 | 任务 | 工作量 |
|------|------|--------|
| P0 | 运行内容检查脚本，修复发现的问题 | 1 天 |
| P1 | 为 Python 24 关手动添加知识点标签、预估时间、前置知识点 | 1.5 天 |
| P1 | 个性化推荐算法：基于知识掌握度和错题，计算下一题推荐，编写推荐 API | 1.5 天 |
| P1 | 改造管理后台：新增知识点标签管理界面 | 1 天 |
| P1 | 学习周报统计逻辑：完成关卡数/学习时长/常见错误/薄弱知识点统计 | 1 天 |
| 收尾 | 编写项目文档（README、架构说明、演示流程） | 1 天 |
| 收尾 | 全面测试整个流程，准备演示数据，修复发现的 bug | 1 天 |

**交付物：**
- 完整带标签的 Python 关卡数据（24 关）
- 推荐算法服务
- 改造后的管理后台
- 项目文档
- 测试报告和演示数据

**独立完成度：** 90% 独立。标签可以一个人慢慢加，推荐算法只依赖数据库结构。

---

## 八、合作点详细说明

全体合作点仅有 5 个，总沟通时间约 2 小时，其余时间各自独立工作。

---

### 合作点 1：接口定义会议（全体，约 30-45 分钟）

**目的：** 定下所有 API 接口的输入输出格式，之后各人按约定开发，不需要再沟通。

**需要确定的接口：**

**能力诊断接口（A 实现后端，C 实现前端）**

```
POST /api/v1/diagnostic/submit
请求体：{ "answers": [{"question_id": 1, "answer": "A"}, ...] }
响应体：{
  "score": 75,
  "skill_level": "intermediate",
  "correct_tags": ["变量", "print"],
  "weak_tags": ["循环", "函数"],
  "recommended_start": "python-05-loops",
  "message": "你已经掌握了变量和输出，但循环还需要加强"
}
```

**代码审查接口（B 实现后端，C 实现前端）**

```
POST /api/v1/ai/review
请求体：{ "lesson_id": 1, "code": "def hello():\n    return 'hello'" }
响应体：{
  "correctness": 85,
  "readability": 70,
  "efficiency": 65,
  "compliance": 80,
  "problems": [
    {"line": 1, "severity": "warning", "message": "函数名可读性..."}
  ],
  "summary": "整体代码逻辑正确...",
  "next_action": "建议重新练习..."
}
```

**错题本接口（B 实现后端，C 实现前端）**

```
GET /api/v1/errors?type=syntax
响应体：{
  "errors": [
    {"id": 1, "lesson_title": "变量与数据类型", "error_type": "syntax",
     "error_code": "print(name", "ai_analysis": "缺少右括号", "is_resolved": false}
  ],
  "stats": {"syntax": 5, "logic": 3, "boundary": 1, "performance": 0}
}
```

**推荐接口（D 实现后端，C 实现前端）**

```
GET /api/v1/lessons/recommend
响应体：{
  "recommended": [
    {"slug": "python-05-loops", "title": "循环", "reason": "你的薄弱知识点"}
  ],
  "next_normal": {"slug": "python-06", "title": "列表"},
  "knowledge_map": {"循环": 35, "函数": 82, "变量": 90}
}
```

**知识掌握度接口（A 实现后端，C 实现前端）**

```
GET /api/v1/progress/knowledge
响应体：{
  "knowledge": [
    {"tag": "变量", "mastery": 90, "total_attempts": 5},
    {"tag": "循环", "mastery": 35, "total_attempts": 8},
    {"tag": "函数", "mastery": 60, "total_attempts": 3}
  ]
}
```

**会议输出：** 一份 `API_CONTRACT.md` 文档，所有接口的输入输出格式写清楚，每个人拍照存证。

---

### 合作点 2：数据库表设计评审（A 主导，全体参与，约 15 分钟）

**目的：** 确认三张新表的字段设计，确保每个人的需求都被覆盖。

**A 需要提前准备：** `UserDiagnostic` 表字段设计、`UserKnowledge` 表字段设计、`UserError` 表字段设计。

**评审内容：** 每个人提出自己需要的字段 → A 确认是否已包含 → 确认后 A 开始建表。

**会议输出：** A 更新 `models/` 代码，提交到 Git，其他人拉取。

---

### 合作点 3：知识点标签规范（D 主导，A 参与，约 10 分钟）

**目的：** 定下标签命名规范，确保标签名称一致。

**需要确定：**
- 标签名称规范：用"循环"还是"for循环"？用"条件判断"还是"if语句"？
- 标签分类：基础语法、数据结构、函数、面向对象、异常处理等
- 标签粒度：一个关卡最多几个标签？

**建议规范：** 标签用中文，粒度适中，如：变量、数据类型、运算符、条件判断、循环、列表、字典、函数、类、异常处理、文件操作。每个关卡 2-4 个标签。前置知识点也用同样的标签体系。

**会议输出：** 一份标签列表，D 开始按这个列表打标签。

---

### 合作点 4：前端 UI 方向评审（C 主导，全体参与，约 20 分钟）

**目的：** 确认 Hello World 的 UI 风格方向，避免 C 做完后大家不满意。

**C 需要提前准备：**
- 配色方案（从深色科技风改为清新蓝白风格，更符合初学者友好定位）
- 关键页面线框图（诊断页面、审查面板、错题本页面）
- 组件库选择（用 TailwindCSS 自绘还是引入 Chart.js）

**会议输出：** 方向确认，C 开始开发。

---

### 合作点 5：最终联调（全体，约半天）

**目的：** 按主流程走一遍，修复发现的问题。

**联调流程：**
```
注册 → 登录 → 选择学习目标
    → 能力诊断 → 查看诊断结果 → 进入推荐关卡
    → 编写代码 → 提交 → 查看 AI 审查结果（雷达图 + 逐行标注）
    → 故意做错 → 查看错题本 → 做对 → 错题标记为已解决
    → 查看知识掌握度更新 → 查看推荐下一关
    → 个人中心 → 查看学习周报
```

**联调方式：** 一个人操作演示，其他人看自己的模块是否正常，发现问题当场记录，当场分配修复。

**会议输出：** Bug 列表，各人修复自己的模块。

---

## 九、并行性与等待分析

### 9.1 真实依赖图

```
                    ┌─────────────┐
                    │ A：基础设施  │ ← 关键路径，其他人依赖 A 的数据库
                    └──────┬──────┘
                           │
            ┌──────────────┼──────────────┐
            ▼              ▼              ▼
    ┌───────────┐  ┌───────────┐  ┌───────────┐
    │ B：AI服务  │  │ C：前端    │  │ D：内容    │
    │ 独立性强   │  │ 可用 Mock │  │ 独立性强   │
    └───────────┘  └───────────┘  └───────────┘
```

### 9.2 真实等待分析

| 等待点 | 谁等谁 | 等什么 | 持续时间 | 能否消除等待 |
|--------|--------|--------|----------|--------------|
| 1 | B、C、D 等 A | A 建好三张数据表，才能开始写真实的数据操作代码 | A 需要 1 天 | **可以消除**：A 第一天就能完成建表 |
| 2 | C 等 A | C 的能力诊断页面需要 A 的诊断 API 上线才能联调 | A 需要 2-3 天 | **可以消除**：C 第一天约定接口格式，用 Mock 数据开发，接口好了再切换 |
| 3 | C 等 B | C 的雷达图需要 B 的结构化审查 API 返回真实数据 | B 需要 3-4 天 | **可以消除**：C 用硬编码的 Mock JSON 先开发雷达图组件，等 B 好了再切换。接口格式第一天就定好了 |
| 4 | D 等 A | D 的推荐算法需要查询 `UserKnowledge` 表 | A 第二天完成 | **可以消除**：D 第一天可以先打标签，A 的表好了再写推荐算法 |

### 9.3 第一周实际并行安排（没有人需要等待）

```
第一周第一天：
  A：开始 P0 修复（改代码）
  B：开始 AI 抽象层（写代码，不依赖数据库）
  C：UI 重命名 + 绘制诊断页面静态稿（纯前端，不依赖接口）
  D：编写内容检查脚本（纯 Python，不依赖其他模块）
  + 接口定义会议（30分钟）

第一周第二天：
  A：P0 完成 → 三张表建完 ✅（解除第一个等待点）
  B：继续 AI 抽象层（仍然独立）
  C：用 Mock 数据开发诊断页面（知道接口格式，不需要真实 API）
  D：运行内容检查脚本，修复发现的问题

第一周第三至五天：
  A：写诊断 API（其他人不需要等，因为 C 用 Mock）
  B：分层 AI 导师 Prompt（独立）
  C：诊断页面完成 → 错题本页面（继续 Mock）
  D：开始打标签（独立，不需要数据库）
```

**结论：第一周四个人完全并行，没有真正的阻塞等待。** 所有"等待"都可以通过"约定接口 + Mock 数据"消除。第二周开始，A 的 API 已经就绪，C 和 D 可以开始对接真实数据。

---

## 十、风险评估与降级方案

### 10.1 最可能的风险

| 风险 | 严重程度 | 发生概率 | 影响 | 降级方案 |
|------|----------|----------|------|----------|
| B 的 AI Prompt 无法让 DeepSeek-V4-Flash 返回正确 JSON 格式 | 高 | 中 | 结构化审查解析失败，雷达图无法显示 | 降级：审查返回普通文本（不解析 JSON），雷达图隐藏，不影响其他功能 |
| C 的雷达图组件无法正常工作 | 中 | 中 | 可视化效果达不到预期 | 降级：用简单的条形图代替雷达图，或者用纯 CSS 进度条展示每个维度得分 |
| D 的推荐算法逻辑正确但推荐不准 | 低 | 中 | 推荐结果不智能，用户觉得没用 | 降级：推荐换成"按顺序推荐下一关"（简单但可靠），把"推荐不准"解释为"数据积累不够" |
| 最后一周发现 Bug 太多修不完 | 中 | 低 | 演示失败 | 降级：提前准备演示数据数据库文件 `data.db`，现场直接用准备好的数据演示 |

### 10.2 为什么风险可控

**核心原因：基础功能已经完整可用。** 258 个关卡、代码评测、AI 对话、排行榜、成就系统——这些都是参考代码已经写好的，不需要你们从零开发。你们做的是"增量改造"而非"从零搭建"。具体来说：

- 即使 B 的 AI 审查出问题 → 至少还有基础的 AI 对话功能（原代码已经有）
- 即使 C 的雷达图出问题 → 至少还有正常的代码评测结果展示
- 即使 D 的推荐算法出问题 → 至少还有 258 关按顺序闯关
- 即使 A 的某张表建错了 → 不影响闯关核心流程

**最坏情况：** 你们演示时，P1 的 6 个创新功能中，4 个运行良好，2 个用降级方案。这完全可接受——因为核心功能（258 关闯关 + 评测 + AI 对话）一直稳定可用。

### 10.3 建议的保险措施

1. **每个人在各自模块保留一个"降级开关"**：一个配置项，如果创新功能出问题，就自动切回原始模式
2. **第二周结束时做一次中期检查**：如果某个功能明显落后，及时砍掉，把精力集中到能完成的功能上
3. **演示时预备"播放 PPT"方案**：把最核心的功能截图放到 PPT 里，万一现场出问题，可以切 PPT 展示

---

## 十一、三周时间线

| 周 | 天数 | 任务 | 责任人 | 里程碑 |
|----|------|------|--------|----------|
| **第一周** | 第 1 天上午 | 环境准备：拉代码 → 每个人跑通后端+前端 | 全体 | 每人本地能运行 |
| | 第 1 天下午 | 合作点 1+2：接口定义会议 + 数据库设计评审 | 全体 | API 契约定稿，数据库设计定稿 |
| | 第 2-3 天 | A：P0 全部修复；B：AI 抽象层；C：UI 重命名；D：内容检查脚本 | 全体并行 | P0 完成，基础骨架可用 |
| | 第 4-5 天 | A：建表 + 诊断后端；B：分层 AI 导师；C：诊断页面；D：开始打标签 | 全体并行 | 核心模块骨架完成 |
| | 周末 | 合作点 3+4：标签规范（10 分钟）+ UI 方向（20 分钟） | 全体 | 标签规范定稿，UI 方向确认 |
| **第二周** | 第 1-3 天 | A：会员系统；B：结构化代码审查；C：雷达图 + 错题本页面；D：推荐算法 + 继续打标签 | 全体并行 | 主要功能完成 70% |
| | 第 4-5 天 | A：联调准备；B：错题分析；C：知识画像；D：周报统计 + 管理后台 | 全体并行 | 功能基本可用 |
| | 周末 | 各自自测，记录 bug | 全体 | Bug 列表整理完成 |
| **第三周** | 第 1-2 天 | 修 bug，合作点 5：最终联调 | 全体 | 主流程跑通 |
| | 第 3 天 | D：补标签 + 内容检查；C：UI 打磨；B：Prompt 调优；A：文档 | 全体并行 | 内容完整，体验流畅 |
| | 第 4 天 | 准备演示数据，写答辩 PPT | D 牵头，全体 | 演示脚本 + PPT |
| | 第 5 天 | 彩排演示流程，最终修复 | 全体 | 可直接演示 |

---

## 十二、最终产品形态总结

**产品名称：** Hello World — AI 智能体陪你闯关编程学习

**产品形态：** 纯 Web 应用，无需 Docker，双进程启动（后端 FastAPI + 前端 React），用户通过浏览器访问。

**核心差异化（与参考代码对比）：**

| 对比维度 | 参考代码 | Hello World |
|----------|----------|-------------|
| AI 角色 | AI 是被动的聊天框，用户不问就不说话 | **AI 是主动的学习智能体"小智"**，有名字，有记忆，四种模式贯穿全程 |
| AI 模型 | 硬编码 Ollama 本地 `qwen2.5:7b` | **DeepSeek-V4-Flash 云端 API**，更稳定、更快、更便宜 |
| 学习路径 | 固定顺序，每个人都一样 | **AI 诊断后推荐起点，动态调整路径，千人千面** |
| 首次使用 | 注册后直接进首页，没有引导 | **AI 主动打招呼，引导完成能力诊断** |
| 代码审查 | 纯文本评价 | **结构化评分 + 雷达图 + 逐行标注**，一目了然 |
| 错误处理 | 只显示对/错，没有记录 | **AI 错题本，按错误类型分类，追踪改进** |
| 学习进度 | 只统计完成关卡数和经验值 | **知识掌握度画像**，可视化展示"你真正学会了什么" |
| 每周总结 | 无 | **AI 自动生成学习周报** |
| 部署方式 | Docker Compose | **纯本地开发模式**（两个终端启动即可） |

**语言和关卡：** 14 种语言，258 关。Python 24 关有完整的 AI 智能体自适应学习，其他语言保留为固定顺序闯关。保留智能体工坊 8 主线 92 节点，与 AI 智能体联动。

**大改范围明确：**

| 模块 | 照搬？ | 改造？ | 改造程度 |
|------|:------:|:------:|:--------:|
| AI 学习智能体 | ❌ | ✅ | **大改**（全新架构，从被动聊天框 → 主动智能体，DeepSeek-V4-Flash 驱动） |
| Python 闯关 | ❌ | ✅ | **大改**（从固定顺序 → AI 自适应，加标签、诊断、推荐） |
| 智能体工坊 | 照搬 | 联动 | 不改代码，但智能体会推荐这个板块的学习 |
| 其他 13 种语言 | 照搬 | 无改动 | 关卡内容照搬，学习体验被智能体包裹 |
| 代码评测引擎 | 部分照搬 | 部分改造 | **中改**（去掉 Docker 沙箱，改用 subprocess） |
| 前端 UI | ❌ | ✅ | **大改**（全新品牌 "Hello World"，清新蓝白配色，智能体交互界面） |
| 管理后台 | 大部分照搬 | 部分改造 | **小改**（新增知识点标签管理） |
| 排行榜/成就 | 照搬 | 无改动 | 不改 |
| 用户系统 | 部分照搬 | 部分改造 | **小改**（增加会员字段，修复管理员密码） |

**结论：** 完全满足"不能完全照搬照抄参考代码，必须进行一些修改，甚至是大改"的要求。核心创新聚焦于 Python 编程闯关，通过 AI 智能体"小智"将学习体验从"固定顺序闯关"彻底改变为"AI 主动陪伴的自适应学习"。