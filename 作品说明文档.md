# Hello World 闯关式 AI 编程学习平台 — 作品说明文档

> 版本 2.0 ｜ 2026 年 8 月

---

## 一、产品愿景与定位

Hello World 是一个面向编程初学者的**闯关式 AI 编程学习平台**，目标用户为初高中生及机房教学场景。平台通过"闯关 + AI 辅助"的方式，让学生打开浏览器即可零配置开始编程学习。

产品的完整形态包含两大核心板块：

| 板块 | 目标用户 | 核心能力 |
|------|---------|---------|
| **学生学习端** | 初高中生 | 172关闯关学习、AI智能体辅导、代码评测、能力诊断、错题本 |
| **教师创作工具** | 教师/管理员 | Markdown教程编辑器、题库管理系统、文件导入解析、教程-题目关联 |

**产品定位：** 从"只面向学生"升级为"学生 + 教师双端协同"，教师可自主编写图文并茂的教程、管理题库，形成完整的教学闭环。面向机房教学场景，学生端零配置，服务端统一维护运行时环境，实现"50 台机器的环境配置 → 1 台服务器的部署"的效率提升。

### 平台核心指标

| 指标 | 数值 |
|------|------|
| 闯关关卡 | 172 道 |
| 编程语言 | 6 种（Python / JavaScript / Java / C / C++ / TypeScript） |
| AI 智能体模式 | 4 种（诊断 / 导师 / 审查 / 规划） |
| 智能体工坊主线 | 8 条，92 个节点 |
| 数据库表 | 18 张 |
| API 端点 | 40+ 个 |

---

## 二、学生学习端（已实现）

### 2.1 闯关学习

平台为 6 种编程语言设计了总计 172 道闯关关卡。每道关卡包含：内容标题、题目描述、难度等级、XP 奖励、起始代码、测试用例、提示信息和知识点标签。关卡采用**顺序解锁机制**：第 1 关始终可用，后续关卡必须前置通关后自动解锁。用户完成关卡后，系统自动结算得分、评定星级（0-5 星）并增加 XP，每 100 XP 升一级。

| 语言 | 关卡数 |
|------|-------|
| Python | 34 |
| JavaScript | 30 |
| Java | 30 |
| C | 20 |
| C++ | 28 |
| TypeScript | 30 |
| **合计** | **172** |

### 2.2 能力诊断

注册用户可在课程大厅入口进行能力诊断。系统包含 10 道硬编码选择题，覆盖 Python 核心知识点。提交答案后后端纯代码评分，根据得分判定能力等级（入门级/进阶级/高级）并推荐学习起点。诊断结果存入数据库并在前端展示。

**评分规则：** 0-30 分入门级（从第一关开始），31-60 分入门级（可跳过基础关），61-80 分进阶级（从循环/函数开始），81-100 分高级（挑战高级内容）。

### 2.3 AI 智能体"小智"

AI 助手支持 4 种角色模式：

- **诊断模式**：分析代码错误，逐条列出问题并给出修复建议
- **导师模式**：用启发式方式引导学习者自己发现问题，不直接给答案
- **审查模式**：从正确性、可读性、性能、健壮性四个维度评分并展示雷达图
- **规划模式**：根据知识掌握情况推荐学习方向

系统采用抽象工厂模式设计 AI 提供者（DeepSeek API 主调用 + Mock 降级方案），实现了 WebSocket 流式对话和对话历史数据库持久化。

### 2.4 代码评测引擎

平台自主研发了一套代码评测引擎（`judge_service.py`），支持 6 种编程语言的代码编译和执行。用户提交代码后，后端查出测试用例，将代码与用例拼接后通过 subprocess 模块启动子进程执行。执行完成后通过智能比对方法（精确匹配、数值近似 <0.0001、子串匹配、空白归一化、numpy 前缀清洗）判断结果。评测**完全不依赖 AI**，AI 仅在代码出错后做错误分类时介入。

### 2.5 知识掌握度与推荐

系统基于加权平均算法（新掌握度 = 当前得分 × 0.6 + 历史掌握度 × 0.4）自动更新用户的知识点掌握度。推荐算法为规则驱动方式：找出用户掌握度最低的知识标签→筛选覆盖该标签的未完成关卡→按掌握度升序推荐最多 3 条。新用户无匹配时返回下一节顺序学习关卡作为兜底。

### 2.6 错题本与双轨降级

每次提交未拿满分时，系统自动记录代码和错误信息，并调用 AI 进行错误分类（syntax / logic / boundary / performance）。如果 AI 调用因网络原因失败，自动降级到规则关键词匹配分类。错题本支持按类型筛选、AI 分析展开查看、标记已解决和重新挑战。

### 2.7 会员系统

平台实现了 Mock 会员系统，设计 Free 和 Pro 双方案：免费用户每天可使用 AI 智能体 5 次，Pro 用户不限次数。该系统为演示功能，未接真实支付。

### 2.8 智能体工坊

平台额外提供了 8 条 AI 学习主线，以神经元地图形式展示共计 92 个学习节点：

| 主线 | 节点数 |
|------|-------|
| 机器学习 | 8 |
| Agent 开发 | 28 |
| 大模型应用 | 8 |
| 综合项目 | 8 |
| 深度学习 | 12 |
| 自然语言处理 | 10 |
| 计算机视觉 | 10 |
| 强化学习 | 8 |

节点之间通过连线连接形成可视化路径，每个节点设置五级能量评分系统（理解度、实现度、优化度、创新度四个维度）。

---

## 三、教师创作工具（预期产品核心新增）

教师创作工具是产品从"纯学生端"升级为"教学闭环"的关键模块。教师可通过管理后台自主编写图文并茂的教程、管理题库，实现教学内容的持续迭代。该模块采用分步实施方案，逐步交付。

### 3.1 整体方案设计

教师创作工具分为三个步骤，按优先级逐步实施：

| 步骤 | 优先级 | 周期 | 目标 |
|------|:------:|:----:|------|
| 第一步：Markdown 教程编辑器 | **P0** | 1 周 | 教师可编写图文并茂的教程，支持图片粘贴/拖拽/选择上传 |
| 第二步：题库管理系统 | **P0** | 2 周 | 题库从 JSON 迁移到数据库，支持批量导入导出、教程-题目关联 |
| 第三步：文件导入解析 | **P1** | 2-3 周 | 教师上传 PDF/Word 文件，系统自动解析为教程内容 |

**为什么选 Markdown 而非富文本编辑器：**

| 对比维度 | Markdown 编辑器 | 富文本编辑器 |
|---------|:--:|:--:|
| 开发时间 | 1 周 | 2-3 周 |
| 存储格式 | 纯文本，数据库友好 | HTML/JSON，体积大 |
| 与现有系统兼容 | `content` 字段已是 Markdown，零改动 | 需改 Schema 和渲染管线 |
| 代码块支持 | 原生支持，语法高亮易扩展 | 需额外插件 |

### 3.2 第一步：Markdown 教程编辑器（P0）

**功能概述：** 教师在管理后台使用 Markdown 编辑器编写教程，左侧编辑区 + 右侧实时预览，支持图片的多种输入方式。

**编辑器能力：**
- 工具栏：H1-H6 标题、加粗、斜体、行内代码、代码块（带语言选择）、有序/无序列表、链接、图片
- 实时预览：使用 `markdown-it` 渲染，支持 GFM 扩展（表格、任务列表）
- 图片上传：支持工具栏按钮选择文件、拖拽到编辑区、Ctrl+V 粘贴三种方式

**图片上传安全设计：**
- 校验管理员权限（`require_role("editor")` 依赖注入）
- 限制文件大小 ≤ 5MB
- 服务端通过 magic bytes 校验真实文件类型（jpg/png/gif/webp），不信任浏览器 MIME 类型
- 禁止 SVG（可携带脚本）
- 服务端生成 UUID 文件名，按日期分目录存储：`static/uploads/{YYYYMMDD}/{uuid}.{ext}`
- 返回可访问 URL 供 Markdown 嵌入

**学生端渲染增强：**
- 引入 `highlight.js` 实现代码块语法高亮
- 启用 GFM 扩展（表格、任务列表）
- 确保 `/uploads/...` 相对路径图片正常显示

**改动范围：**
- 新增图片上传 API（`POST /api/v1/admin/lessons/upload-image`）
- 课程核心表（`lessons`）Schema 不变，`content` 字段继续存储 Markdown
- 现有 CRUD API 不变，仅新增上传接口
- 学生端渲染核心逻辑不变，仅增强

### 3.3 第二步：题库管理系统（P0）

**功能概述：** 题库从 JSON 文件迁移到数据库，教师可通过管理后台独立管理题库，支持批量导入导出，题目与教程多对多关联。

**数据源策略：**
- 历史 JSON 文件保留作为**初始化数据 + 备份**，仅在首次部署或重置时导入
- **数据库是教师维护后内容的唯一事实来源**
- 教师通过管理后台的所有增删改操作直接写入数据库，不回写 JSON
- 系统重启不再执行 JSON → 数据库的自动覆盖导入（避免覆盖教师的修改）

**数据库设计：**

```
questions 表（独立题库）
├── id
├── language_id（外键）
├── title
├── description
├── difficulty
├── test_cases (JSON)
├── knowledge_tags (JSON)
├── source（手动/导入）
├── created_by (admin_id)
└── created_at, updated_at

lesson_questions 关联表（教程与题目多对多）
├── lesson_id
├── question_id
└── order
```

**题库管理功能：**
- 题目列表（分页、按语言/知识点/难度筛选、关键词搜索）
- 新增/编辑/删除题目（含测试用例 JSON 编辑器、知识点标签管理）
- Excel/CSV 批量导入（含模板下载）
- 批量导出（按语言/知识点/难度筛选）

**批量导入预检查机制：**
- 上传 Excel/CSV 后不直接入库，先返回预检查报告
- 校验项：必填字段缺失、slug 重复、无效测试用例格式、知识点标签不在允许范围、难度值不在枚举范围
- 前端展示错误行号、字段名和错误原因
- 全部无错误后才允许确认入库
- 导入过程使用数据库事务，失败时整体回滚

**教程-题目关联：**
- 编辑关卡时可选择关联已有题库题目（多选组件，按语言/知识点筛选）
- 一道题可关联到多个教程关卡

### 3.4 第三步：PDF/Word 文件导入（P1，按需启动）

**功能概述：** 教师上传 PDF/Word/Markdown 文件，系统自动解析为教程内容填入编辑器。

| 文件类型 | 解析方案 | 预期效果 |
|---------|---------|---------|
| Markdown | 直接读取文件内容 | 完美还原 |
| Word (.docx) | `python-docx` 解析段落、图片、表格 | 基本文本+图片+表格可较好还原 |
| PDF | `PyMuPDF` 提取文本和图片 | 效果有限，保留原始文件供下载 |

**触发条件：** 前两步完成后，验证教师确实有文件导入需求。如果教师愿意使用 Markdown 编辑器，这一步可简化或不做。

**PDF 解析的现实预期：** PDF 解析是行业性难题，Google 和微软做了十几年都未完全解决。系统引导教师优先使用 Markdown 编辑器直接编写，PDF 仅作为辅助参考格式，解析结果保留原始文件供下载，不追求 100% 还原。

---

## 四、关于竞赛题库 RAG 的决策

经技术可行性分析，**竞赛题库 RAG 暂不做**。核心理由：

1. **内容不匹配**：竞赛题（LeetCode Medium/Hard、洛谷普及/提高）难度远超初高中生入门水平，RAG 检索结果学生用不上。这是方向性错误，不是技术实现问题。
2. **版权风险**：爬取商业题库平台存在法律风险，商业产品尤其需要谨慎。
3. **维护成本高**：反爬机制需要持续对抗，4 人团队投入产出比极低。

**远期方向：** 如果将来要做 RAG，正确的方向是用教师上传的教程内容做知识库。内容天然匹配教学场景，零版权风险，可随教师上传逐步积累。需从零搭建完整检索链路（Embedding 验证 → ChromaDB → 检索排序 → 结果注入），约 2-3 周，且必须实现按学校/教师的内容隔离。

---

## 五、技术架构

### 5.1 系统架构全景

采用五层架构设计：

```
┌─────────────────────────────────────────────────┐
│  用户接入层                                      │
│  学生浏览器 · 教师/管理员浏览器                    │
├─────────────────────────────────────────────────┤
│  前端层                                          │
│  React 学生端 (Port 5173)                        │
│  React 管理后台 (Port 5174)                      │
│  TypeScript · Vite · TailwindCSS                 │
│  Zustand · Axios · Monaco Editor · markdown-it   │
├────────── API 请求 (/api/v1/*) ─────────────────┤
│  后端网关层                                      │
│  FastAPI · Uvicorn (ASGI) · Port 8006            │
│  JWT 认证 · SQLAlchemy · Pydantic · CORS         │
├─────────────────────────────────────────────────┤
│  业务服务层                                      │
│  AI 智能体 · 代码评测 · 认证与用户               │
│  图片上传 · 题库管理 · 文件解析                   │
│  DeepSeek/Mock · Subprocess · Semaphore          │
├─────────────────────────────────────────────────┤
│  数据与外部资源层                                │
│  SQLite · DeepSeek API · 文件系统 (uploads/)     │
└─────────────────────────────────────────────────┘
```

### 5.2 前端技术栈

| 技术 | 选择 | 设计意图 |
|------|------|---------|
| 框架 | React 19 + TypeScript 6 | 现代化开发体验，类型安全 |
| 构建工具 | Vite 8 | 极速 HMR，开发效率优先 |
| 样式方案 | TailwindCSS 4 | 原子化 CSS，按需生成 |
| 状态管理 | Zustand 5 | 轻量、无模板代码 |
| HTTP 客户端 | Axios | 拦截器管理 Token，统一错误处理 |
| 代码编辑器 | Monaco Editor | VS Code 同款内核，支持 14 种语言语法高亮 |
| 图表库 | Chart.js | 雷达图展示 AI 四维评分 |
| Markdown 渲染 | markdown-it + highlight.js | 关卡内容渲染 + 代码语法高亮 |
| UI 组件库 | Ant Design | 管理后台表格、表单、上传等组件 |

### 5.3 后端服务架构

FastAPI 应用生命周期：启动时自动执行 `init_db()` → `seed_database()` → `seed_demo_data()`。内置 CORS 中间件、请求日志中间件、全局异常处理器、JWT 认证依赖和基于滑动窗口的速率限制。

### 5.4 AI 架构设计

AI 智能体采用**抽象工厂模式**：`AIProvider` 抽象基类定义 `chat` 和 `stream` 两个核心方法，`DeepSeekProvider`（主，支持 429/503 自动重试 3 次，120s 超时）和 `MockProvider`（降级）为具体实现。

**双层 Prompt 组合：** 模式级 Prompt（4 种模式各有专用 System Prompt）× 语言级 Prompt（6 种语言各配导师和审查官两套提示词），调用时自动拼接。

---

## 六、数据模型

数据库共 **18 张表**，以 SQLite 为存储引擎，通过 SQLAlchemy ORM 进行异步操作（aiosqlite）。

### 核心表说明

| 表名 | 核心字段 | 功能说明 |
|------|---------|---------|
| `users` | username, hashed_password, level, xp, membership, ai_usage_today, streak_days | 用户账号、等级经验值、会员身份 |
| `languages` | name, slug, description, color, is_active | 6 种编程语言元数据 |
| `lessons` | title, slug, language_id, order, difficulty, test_cases, knowledge_tags, prerequisites, estimated_minutes | 172 道闯关关卡 |
| `questions` | language_id, title, slug, description, difficulty, question_type, test_cases, knowledge_tags, source | 独立题库，支持教师批量管理（6 种题型） |
| `lesson_questions` | lesson_id, question_id, order | 教程与题目多对多关联 |
| `progress` | user_id, lesson_id, status, best_score, attempts | 学习进度 |
| `submissions` | user_id, lesson_id, code, language, status, score, stderr, ai_feedback | 代码提交记录 |
| `user_diagnostics` | user_id, score, skill_level, correct_tags, weak_tags | 能力诊断结果 |
| `user_knowledge` | user_id+knowledge_tag, mastery | 知识点掌握度 |
| `user_errors` | user_id, lesson_id, error_code, error_type, ai_analysis | 错题记录 |
| `chat_histories` | user_id, lesson_id, messages (JSON) | AI 对话历史 |
| `neuron_nodes` | title, slug, track, test_cases, energy_levels | 智能体工坊节点 |
| `agent_progress` | user_id, node_id, status, energy_score, energy_detail | 智能体节点进度 |
| `admin_users` | username, role (admin/editor/viewer) | 管理后台账号 |

---

## 七、API 概览

> 完整契约见 [API接口契约.md](API接口契约.md)

### 学生端 API

| 类别 | 端点 | HTTP 方法 |
|------|------|:----------:|
| 认证 | `/api/v1/auth/register`, `/login`, `/refresh` | POST |
| 课程 | `/api/v1/languages`, `/languages/{slug}/map` | GET |
| 课时 | `/api/v1/lessons/{id}`, `/lessons/{id}/submit`, `/lessons/recommend` | GET/POST |
| AI | `/api/v1/ai/chat`, `/ai/tutor`, `/ai/review`, `/ai/plan`, `/ai/classify-error` | POST |
| AI 流式 | `/api/v1/ai/chat/ws?token={jwt}` | WebSocket |
| 诊断 | `/api/v1/diagnostic/questions`, `/submit`, `/result` | GET/POST |
| 错题 | `/api/v1/errors`, `/errors/{id}/resolve` | GET/PATCH |
| 知识 | `/api/v1/progress/knowledge`, `/users/me/activity` | GET |
| 智能体 | `/api/v1/agent/map`, `/nodes/{id}`, `/nodes/{id}/submit`, `/tracks` | GET/POST |
| 会员 | `/api/v1/users/me/membership`, `/users/me/upgrade` | GET/POST |

### 教师端 API（已实现，P1 文件解析除外）

| 类别 | 端点 | HTTP 方法 | 说明 |
|------|------|:----------:|------|
| 图片上传 | `/api/v1/admin/lessons/upload-image` | POST | 教程图片上传，含安全校验 |
| 题库管理 | `/api/v1/admin/questions` | GET/POST | 题目列表 + 新增 |
| 题库管理 | `/api/v1/admin/questions/{id}` | GET/PUT/DELETE | 题目详情/编辑/删除 |
| 批量导入 | `/api/v1/admin/questions/import` | POST | Excel/CSV 导入，含预检查 |
| 批量导出 | `/api/v1/admin/questions/export` | GET | 按条件导出 Excel |
| 教程关联 | `/api/v1/admin/lessons/{id}/questions` | GET/POST | 教程-题目关联管理 |
| 文件解析 | `/api/v1/admin/lessons/parse-word` | POST | Word 解析为 Markdown（P1） |
| 文件解析 | `/api/v1/admin/lessons/parse-pdf` | POST | PDF 解析为 Markdown（P1） |

---

## 八、代码评测引擎

### 8.1 评测流程

1. 用户点击运行，前端将代码和关卡编号发送到后端
2. 后端查询该关卡的测试用例
3. 将用户代码与测试用例拼接
4. 通过 subprocess 启动子进程执行代码
5. 捕获标准输出、标准错误和返回码
6. 通过 `_smart_match` 智能比对实际输出与期望输出
7. 计算得分、评定星级（0-5 星）、增加 XP

### 8.2 多语言执行配置

| 语言 | 扩展名 | 执行命令 | 编译步骤 |
|------|--------|----------|---------|
| Python | .py | `python {file}` | 无 |
| JavaScript | .js | `node {file}` | 无 |
| Java | .java | `java -cp {dir} {classname}` | `javac -encoding UTF-8` |
| C | .c | `{output}` | `gcc -std=c11 -o` |
| C++ | .cpp | `{output}` | `g++ -std=c++17 -o` |
| TypeScript | .ts | `node {js_output}` | `tsc --target ES2020` |

### 8.3 智能比对

`_smart_match` 支持：精确匹配、数值近似（浮点误差 <0.0001）、子串匹配、空白归一化、numpy 前缀清洗。这些模式按优先级尝试，任一匹配即判定通过。

---

## 九、创新亮点

### 9.1 自研代码评测引擎

平台从零开发了一整套评测引擎，支持 6 种编程语言的编译和执行、智能输出匹配算法、多语言兼容性优化。评测完全不依赖 AI 大模型。

### 9.2 双轨降级 AI 错误分类

AI 错误分类采用"AI 主分类 + 规则降级"的双轨机制。AI 不可用时自动降级到规则关键词匹配，确保错题分类功能在任何情况下都不会完全失效。

### 9.3 规则驱动推荐系统

推荐系统基于知识掌握度的规则驱动算法，非黑盒机器学习模型。每一步推荐都可以解释原因，同时设计了完备的兜底策略。

### 9.4 六语言统一执行框架

通过 `language_config.py` 将 6 种语言的编译和执行命令抽象为统一配置。新增语言时只需添加配置项，无需修改评测流程代码。

### 9.5 机房部署优化

面向教育机房场景设计：学生端零配置（仅需浏览器），所有编程运行时由服务端统一维护。

### 9.6 神经元可视化

智能体工坊以神经元地图形式展示 8 条 AI 学习主线，92 个节点连线形成可视化学习路径。

### 9.7 教师创作工具 — 产品闭环

教师创作工具使产品从"内容固定"升级为"教师可自主创作"。教师通过 Markdown 编辑器编写图文教程，通过题库管理系统维护习题，与教程灵活关联，形成完整的教学闭环。这是产品进入学校场景的核心能力。

### 9.8 渐进式交付策略

采用分步实施：第一步 Markdown 编辑器 1 周 MVP，第二步题库管理 2 周，第三步文件导入按需启动。每一步做完都能独立使用，不需要等三步全做完才有价值。

---

## 十、产品路线图

| 优先级 | 模块 | 周期 | 状态 | 交付物 |
|:------:|------|:----:|:----:|------|
| **已完成** | 学生学习端（闯关、AI、评测、诊断、错题本、工坊） | 4 周 | 已完成 | 172关、6语言、4种AI模式 |
| **已完成** | Markdown 教程编辑器 + 图片上传 | 1 周 | 已完成 | 编辑器组件（`MarkdownEditor.tsx`）、图片上传API（`upload.py`）、LessonEditor 集成 |
| **已完成** | 题库管理系统（数据库迁移 + 批量导入导出 + 教程关联） | 2 周 | 已完成 | 题库 CRUD（`questions.py`）、批量导入预检查（`import_service.py`）、教程-题目关联（`lesson_questions.py`） |
| **未完成** | 学生端渲染增强（代码语法高亮 + GFM 扩展） | 0.5 周 | 未完成 | highlight.js 集成、markdown-it 插件（表格/任务列表） |
| **未完成** | Seed 服务改造（仅首次初始化，不覆盖教师修改） | 0.5 天 | 未完成 | lessons 覆盖逻辑改为首次检测 |
| **P1** | PDF/Word 文件导入解析 | 2-3 周 | 按需启动 | python-docx + PyMuPDF 解析 |
| **P2** | 教师内容 RAG（远期） | 2-3 周 | 远期规划 | 需先验证 Embedding 服务可用性 |
| **不做** | 竞赛题库 RAG | — | 已否决 | 内容不匹配、版权风险、维护成本高 |

---

## 十一、启动与部署

### 环境要求

- Python 3.11+
- Node.js 20+

### 启动步骤

```bash
# 1. 启动后端（自动建表和种子数据）
cd HelloWorld-api
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8006

# 2. 启动学习端
cd HelloWorld-web
npm install
npx vite --port 5173 --host

# 3. 启动管理后台
cd HelloWorld-admin-web
npm install
npm run dev
```

- 学习端地址：http://localhost:5173
- 管理后台地址：http://localhost:5174（管理员 admin/admin123）
- 演示账号：demo / demo123

---

## 十二、团队分工

| 成员 | 角色 | 核心任务 |
|:----:|:----:|----------|
| **组长** | 项目统筹 | 系统架构设计、后端核心开发、答辩材料统筹 |
| **成员 A** | 后端核心 | 图片上传 API、题库数据库设计与迁移、Crud API、Seed 逻辑改造、Word/PDF 解析 |
| **成员 B** | 管理后台前端 | Markdown 编辑器组件、工具栏、图片上传前端集成、题库管理页面、文件导入前端 |
| **成员 C** | 全栈集成 | 学生端渲染增强、LessonEditor 替换、批量导入预检查后端、教程-题目关联 API、批量导出 |
| **成员 D** | 测试与质量保障 | 安全测试、功能测试、数据迁移测试、集成测试、端到端验证 |