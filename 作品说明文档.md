# Hello World 闯关式 AI 编程学习平台 — 作品说明文档

> 版本 1.0 ｜ 2026 年 7 月

---

## 一、项目背景与定位

Hello World 是一个面向编程初学者的**闯关式 AI 编程学习平台**。项目旨在解决传统编程学习中两大核心痛点：环境配置门槛高（需要逐台安装 Python、JDK、Node.js 等开发环境）和学习路径不清晰（初学者面对海量资源不知道从何入手）。平台通过"闯关 + AI 辅助"的方式，让用户打开浏览器即可零配置开始编程学习。

| 指标 | 数值 |
|------|------|
| 闯关关卡 | 172 道 |
| 编程语言 | 6 种 |
| AI 智能体模式 | 4 种 |
| 数据库表 | 18 张 |
| API 端点 | 40+ 个 |
| 智能体工坊节点 | 92 个 |

平台采用前后端分离架构：**React 19 + TypeScript + Vite 8** 构建双前端（学习端 + 管理后台），**FastAPI + SQLAlchemy + SQLite** 作为后端服务，**DeepSeek API** 提供 AI 能力。面向机房教学场景，学生端零配置，服务端统一维护运行时环境，实现"50 台机器的环境配置 → 1 台服务器的部署"的效率提升。

---

## 二、核心功能模块

### 2.1 闯关学习

平台为 6 种编程语言设计了总计 172 道闯关关卡。每道关卡包含：内容标题、题目描述、难度等级、XP 奖励、起始代码、测试用例、提示信息和知识点标签。关卡采用**顺序解锁机制**：第 1 关始终可用，后续关卡必须前置通关后自动解锁。用户完成关卡后，系统自动结算得分、评定星级（0-5 星）并增加 XP，每 100 XP 升一级。

| 语言 | 关卡数 |
|------|-------|
| Python | 34 |
| JavaScript | 30 |
| Java | 30 |
| C | 20 |
| C++ | 28 |
| TypeScript | 30 |
| **合计** | **172** |

### 2.2 能力诊断

注册用户可在课程大厅入口进行能力诊断。系统包含 10 道硬编码选择题，覆盖 Python 核心知识点（数据类型、变量、运算符、条件判断、循环、列表、函数、字典、异常处理、文件操作）。提交答案后后端纯代码评分，根据得分判定能力等级（入门级/进阶级/高级）并推荐学习起点。诊断结果存入数据库并在前端展示。

**评分规则：** 逐条比对答案字母，正确数 ÷ 总题数 × 100 = 得分。0-30 分入门级（从第一关开始），31-60 分入门级（可跳过基础关），61-80 分进阶级（从循环/函数开始），81-100 分高级（挑战高级内容）。

### 2.3 AI 智能体"小智"

AI 助手支持 4 种角色模式：

- **诊断模式**：分析代码错误，逐条列出问题并给出修复建议
- **导师模式**：用启发式方式引导学习者自己发现问题，不直接给答案
- **审查模式**：从正确性、可读性、性能、健壮性四个维度评分并展示雷达图
- **规划模式**：根据知识掌握情况推荐学习方向

系统采用抽象工厂模式设计 AI 提供者（DeepSeek API 主调用 + Mock 降级方案），实现了 WebSocket 流式对话和对话历史数据库持久化。

### 2.4 代码评测引擎

平台自主研发了一套代码评测引擎（`judge_service.py`），支持 6 种编程语言的代码编译和执行。用户提交代码后，后端查出测试用例，将代码与用例拼接后通过 subprocess 模块启动子进程执行。执行完成后通过智能比对方法（精确匹配、数值近似 <0.0001、子串匹配、空白归一化、numpy 前缀清洗）判断结果。评测**完全不依赖 AI**，AI 仅在代码出错后做错误分类时介入。

### 2.5 知识掌握度与推荐

系统基于加权平均算法（新掌握度 = 当前得分 × 0.6 + 历史掌握度 × 0.4）自动更新用户的知识点掌握度。推荐算法为规则驱动方式：找出用户掌握度最低的知识标签→筛选覆盖该标签的未完成关卡→按掌握度升序推荐最多 3 条。新用户无匹配时返回下一节顺序学习关卡作为兜底。

### 2.6 错题本与双轨降级

每次提交未拿满分时，系统自动记录代码和错误信息，并调用 AI 进行错误分类（syntax / logic / boundary / performance）。如果 AI 调用因网络原因失败，自动降级到规则关键词匹配分类（SyntaxError→语法，IndexError→边界等）。错题本支持按类型筛选、AI 分析展开查看、标记已解决和重新挑战。

### 2.7 会员系统

平台实现了 Mock 会员系统，设计 Free 和 Pro 双方案：免费用户每天可使用 AI 智能体 5 次，Pro 用户不限次数。用户表 `membership` 字段控制身份，`ai_usage_today` 和 `ai_usage_date` 记录每日用量，超限返回 403 错误提示升级。该系统为演示功能，未接真实支付。

### 2.8 智能体工坊

平台额外提供了 8 条 AI 学习主线，以神经元地图形式展示共计 92 个学习节点：

| 主线 | 节点数 |
|------|-------|
| 机器学习 | 8 |
| Agent 开发 | 28 |
| 大模型应用 | 8 |
| 综合项目 | 8 |
| 深度学习 | 12 |
| 自然语言处理 | 10 |
| 计算机视觉 | 10 |
| 强化学习 | 8 |

节点之间通过连线连接形成可视化路径，每个节点设置五级能量评分系统（理解度、实现度、优化度、创新度四个维度）。

---

## 三、技术架构

### 3.1 系统架构全景

采用五层架构设计：

```
┌─────────────────────────────────────┐
│  用户接入层                          │
│  用户浏览器                          │
├─────────────────────────────────────┤
│  前端层                              │
│  React 用户端 (Port 5173)            │
│  React 管理后台 (Port 5174)          │
│  TypeScript · Vite · TailwindCSS     │
│  Zustand · Axios · Monaco Editor     │
├────────── API 请求 (/api/v1/*) ─────┤
│  后端网关层                          │
│  FastAPI · Uvicorn (ASGI) · Port 8006│
│  JWT 认证 · SQLAlchemy · Pydantic   │
├─────────────────────────────────────┤
│  业务服务层                          │
│  AI 智能体 · 代码评测 · 认证与用户   │
│  DeepSeek/Mock · Subprocess · Semaphore│
├─────────────────────────────────────┤
│  数据与外部资源层                    │
│  SQLite · DeepSeek API · 文件系统    │
└─────────────────────────────────────┘
```

### 3.2 前端技术栈

| 技术 | 选择 | 设计意图 |
|------|------|---------|
| 框架 | React 19 + TypeScript 6 | 现代化开发体验，类型安全 |
| 构建工具 | Vite 8 | 极速 HMR，开发效率优先 |
| 样式方案 | TailwindCSS 4 | 原子化 CSS，按需生成 |
| 状态管理 | Zustand 5 | 轻量、无模板代码 |
| HTTP 客户端 | Axios | 拦截器管理 Token，统一错误处理 |
| 代码编辑器 | Monaco Editor | VS Code 同款内核，支持 14 种语言语法高亮 |
| 图表库 | Chart.js | 雷达图展示 AI 四维评分 |
| Markdown 渲染 | markdown-it | 关卡题目描述渲染 |

### 3.3 后端服务架构

FastAPI 应用生命周期：启动时自动执行 `init_db()` → `seed_database()` → `seed_demo_data()`。内置 CORS 中间件、请求日志中间件、全局异常处理器、JWT 认证依赖和基于滑动窗口的速率限制（登录 5 次/分钟、注册 3 次/分钟、提交 10 次/分钟、AI 20 次/分钟）。

### 3.4 AI 架构设计

AI 智能体采用**抽象工厂模式**：`AIProvider` 抽象基类定义 `chat` 和 `stream` 两个核心方法，`DeepSeekProvider`（主，支持 429/503 自动重试 3 次，120s 超时）和 `MockProvider`（降级，API Key 为空时自动启用）为具体实现。系统启动时按 `AI_PROVIDER_PRIORITY` 优先级自动选择。

**双层 Prompt 组合：** 模式级 Prompt（4 种模式各有专用 System Prompt）× 语言级 Prompt（6 种语言各配导师和审查官两套提示词），调用时自动拼接。

---

## 四、数据模型

数据库共 **18 张表**，以 SQLite 为存储引擎，通过 SQLAlchemy ORM 进行异步操作（aiosqlite）。

### 核心表说明

| 表名 | 核心字段 | 功能说明 |
|------|---------|---------|
| `users` | username, hashed_password, level, xp, membership, ai_usage_today, streak_days | 用户账号、等级经验值、会员身份 |
| `languages` | name, slug, description, color, is_active | 6 种编程语言元数据 |
| `lessons` | title, slug, language_id, order, difficulty, test_cases, knowledge_tags, prerequisites | 172 道闯关关卡 |
| `progress` | user_id, lesson_id, status, best_score, attempts | 学习进度（not_started/in_progress/completed） |
| `submissions` | user_id, lesson_id, code, language, status, score, stderr, ai_feedback | 代码提交记录 |
| `user_diagnostics` | user_id (unique), score, skill_level, correct_tags, weak_tags | 能力诊断结果 |
| `user_knowledge` | user_id+knowledge_tag (unique), mastery (float) | 知识点掌握度，加权平均算法更新 |
| `user_errors` | user_id, lesson_id, error_code, error_type, ai_analysis | 错题记录，AI 分类后写入 |
| `chat_histories` | user_id, lesson_id, messages (JSON) | AI 对话历史持久化 |
| `neuron_nodes` | title, slug, track (8 主线), test_cases, energy_levels | 智能体工坊节点 |
| `agent_progress` | user_id, node_id, status, energy_score, energy_detail | 智能体节点进度 |
| `admin_users` | username, role (admin/editor/viewer) | 管理后台账号，三级角色 |

**枚举类型：** `Difficulty`（beginner/intermediate/advanced）、`SubmissionStatus`（pending/accepted/wrong/error/timeout）、`ProgressStatus`（not_started/in_progress/completed）。

---

## 五、API 概览

> 完整契约见 [API接口契约.md](API接口契约.md)

| 类别 | 端点 | HTTP 方法 |
|------|------|:----------:|
| 认证 | `/api/v1/auth/register`, `/login`, `/refresh` | POST |
| 课程 | `/api/v1/languages`, `/languages/{slug}/map` | GET |
| 课时 | `/api/v1/lessons/{id}`, `/lessons/{id}/submit`, `/lessons/recommend` | GET/POST |
| AI | `/api/v1/ai/chat`, `/ai/tutor`, `/ai/review`, `/ai/plan`, `/ai/classify-error` | POST |
| AI 流式 | `/api/v1/ai/chat/ws?token={jwt}` | WebSocket |
| 诊断 | `/api/v1/diagnostic/questions`, `/submit`, `/result` | GET/POST |
| 错题 | `/api/v1/errors`, `/errors/{id}/resolve` | GET/PATCH |
| 知识 | `/api/v1/progress/knowledge`, `/users/me/activity` | GET |
| 智能体 | `/api/v1/agent/map`, `/nodes/{id}`, `/nodes/{id}/submit`, `/tracks` | GET/POST |
| 会员 | `/api/v1/users/me/membership`, `/users/me/upgrade` | GET/POST |

---

## 六、代码评测引擎

### 6.1 评测流程

1. 用户点击运行，前端将代码和关卡编号发送到后端
2. 后端查询该关卡的测试用例
3. 将用户代码与测试用例拼接（Python 等在代码后追加 print 语句输出结果）
4. 通过 subprocess 启动子进程执行代码
5. 捕获标准输出、标准错误和返回码
6. 通过 `_smart_match` 智能比对实际输出与期望输出
7. 计算得分、评定星级（0-5 星）、增加 XP

### 6.2 多语言执行配置

| 语言 | 扩展名 | 执行命令 | 编译步骤 |
|------|--------|----------|---------|
| Python | .py | `python {file}` | 无 |
| JavaScript | .js | `node {file}` | 无 |
| Java | .java | `java -cp {dir} {classname}` | `javac -encoding UTF-8` |
| C | .c | `{output}` | `gcc -std=c11 -o` |
| C++ | .cpp | `{output}` | `g++ -std=c++17 -o` |
| TypeScript | .ts | `node {js_output}` | `tsc --target ES2020` |

### 6.3 智能比对

`_smart_match` 支持：精确匹配、数值近似（浮点误差 <0.0001）、子串匹配、空白归一化、numpy 前缀清洗。这些模式按优先级尝试，任一匹配即判定通过。

### 6.4 兼容性优化

- **numpy 类型清洗**：正则移除 `np.float64` 等前缀
- **C 语言 % 格式化自适应**：根据期望输出自动选择 `%d` 或 `%s` 格式符
- **Python 多语句拆分**：多语句自动拆分，只打印最后表达式值
- **Windows GBK 编码兼容**：解决 subprocess 在 Windows 下输出乱码的问题

---

## 七、创新亮点

### 7.1 自研代码评测引擎

平台从零开发了一整套评测引擎，支持 6 种编程语言的编译和执行、智能输出匹配算法、多语言兼容性优化。评测完全不依赖 AI 大模型，代码正确性判断全部由规则引擎完成。

### 7.2 双轨降级 AI 错误分类

AI 错误分类采用"AI 主分类 + 规则降级"的双轨机制。正常情况下 AI 进行智能化分类并给出详细分析；AI 不可用时自动降级到规则关键词匹配。确保错题分类功能在任何情况下都不会完全失效。

### 7.3 规则驱动推荐系统

推荐系统基于知识掌握度的规则驱动算法，非黑盒机器学习模型。每一步推荐都可以解释原因，同时设计了完备的兜底策略（新用户回退到顺序学习），保证推荐接口不会返回空结果。

### 7.4 六语言统一执行框架

通过 `language_config.py` 将 6 种语言的编译和执行命令抽象为统一配置，配合代码包装模板实现差异化拼接。新增语言时只需添加配置项，无需修改评测流程代码。

### 7.5 机房部署优化

面向教育机房场景设计：学生端零配置（仅需浏览器），所有编程运行时由服务端统一维护，将"50 台机器逐台配置"缩减为"1 台服务器的部署"。

### 7.6 神经元可视化

智能体工坊以神经元地图形式展示 8 条 AI 学习主线，节点连线形成可视化学习路径。五级能量评分系统从理解度、实现度、优化度、创新度四个维度综合评定。

---

## 八、启动与部署

### 环境要求

- Python 3.11+
- Node.js 20+

### 启动步骤

```bash
# 1. 启动后端（自动建表和种子数据）
cd HelloWorld-api
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8006

# 2. 启动学习端
cd HelloWorld-web
npm install
npx vite --port 5173 --host

# 3. 启动管理后台（可选）
cd HelloWorld-admin-web
npm install
npm run dev
```

- 学习端地址：http://localhost:5173
- 管理后台地址：http://localhost:5174（管理员 admin/admin123）
- 演示账号：demo / demo123

---

## 九、团队分工

| 成员 | 角色 | 核心任务 |
|:----:|:----:|----------|
| **组长** | 项目统筹 | 系统架构设计、后端核心开发、答辩材料统筹 |
| **成员 A** | 后端核心 | 数据库设计、能力诊断、代码评测引擎、知识掌握度、错题本、会员系统 |
| **成员 B** | AI 智能体 | AIProvider 抽象工厂、四种模式、双轨错误分类、WebSocket 流式对话 |
| **成员 C** | 前端开发 | 11 个页面开发、Monaco 编辑器、雷达图、AI 聊天、闯关地图动画 |
| **成员 D** | 内容与推荐 | 172 关课程内容、规则推荐算法、管理后台、课程元数据校验 |
