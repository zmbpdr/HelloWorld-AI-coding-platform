# Hello World — 闯关式 AI 编程学习平台

## 作品简介

Hello World 是一个面向编程初学者的**闯关式 AI 编程学习平台**。用户通过编写代码通过测试用例来闯关获得经验值，平台集成 AI 导师进行实时代码错误分析，Docker 沙箱安全执行用户代码。

平台集成了 **AI 智能体**（诊断/导师/审查/规划四种模式）、**6 种编程语言**（Python 34关 / JS 30关 / Java 30关 / C 20关 / C++ 28关 / TS 30关，共 172 关）、**智能体工坊**（8 条 AI/ML 学习主线，92 个神经元节点）、**能力诊断**、**知识掌握度画像**和 **AI 错题本**。

---

## 核心功能

### 编程闯关（6 语言 · 172 关卡）

| 语言 | 课时 |
|------|------|
| Python | 34 |
| JavaScript | 30 |
| Java | 30 |
| C | 20 |
| C++ | 28 |
| TypeScript | 30 |

- **Monaco 代码编辑器**：支持 14 种语言语法高亮和智能提示
- **智能评测引擎**：6 级匹配策略（精确 → 去空白 → 忽略大小写 → 包含 → 浮点近似 → 去所有空白），支持部分通过按比例得分
- **星级评分**：0-5★，满分且快速执行获 5★
- **评测统计**：每道题的耗时趋势图 + 错误类型分布
- **Subprocess 直接执行**：轻量级本地执行，无需 Docker
- **学习热力图**：GitHub 风格 90 天活跃度可视化
- **代码收藏夹**：⭐ 一键收藏 + 标签分类 + 全文搜索
- **排行榜**：总榜/月榜/周榜 + 前三奖牌

### AI 智能体（四模式）

- **诊断模式**：分析代码语法错误、逻辑缺陷，逐条给出修复建议
- **导师模式**：引导思路、给出提示，不直接给答案
- **审查模式**：四维度（正确性/可读性/性能/健壮性）评分 + 雷达图 + 问题列表
- **规划模式**：根据知识掌握度和学习进度推荐下一步学习路线
- **AIProvider 抽象工厂**：DeepSeek API 主用 → Mock 兜底，无 API Key 也能演示
- 右下角 AI 悬浮球可拖拽，支持四模式切换和流式对话

### 能力诊断 + 知识掌握度 + 错题本

- **能力诊断**：10 道题评估水平，推荐起始关卡
- **知识掌握度**：每次提交自动更新，加权平滑算法（`mastery = score × 0.6 + 历史 × 0.4`），可视化画像 + AI 分析按钮
- **错题本**：失败提交自动记录，AI 错误分类（syntax/logic/boundary/performance），按类型筛选追踪改进

### 智能体工坊（8 主线 · 92 节点）

| 主线 | 节点 | 方向 |
|------|------|------|
| 机器学习 | 8 | 数学基础 → 经典算法 |
| Agent 开发 | 28 | 工具调用 → 多智能体协作 |
| 大模型应用 | 8 | Prompt → RAG 架构 |
| 深度学习 | 12 | 神经网络 → Transformer |
| 自然语言处理 | 10 | 文本预处理 → LLM 微调 |
| 计算机视觉 | 10 | 图像分类 → 目标检测 |
| 强化学习 | 8 | Q-Learning → PPO |
| 综合项目 | 8 | 真实项目实战 |

- 神经元网络地图：前置依赖解锁，按阶段推进
- 能量评级系统：理解力/实现力/优化力/创造力四维评分
- 每个节点独立代码评测

### 管理后台

- 数据仪表盘 + 7 天趋势图
- 用户管理（搜索/封禁/详情）
- 课程管理（14 语言 CRUD + 编辑抽屉）
- 提交审计（按状态/用户名查询 + 异常高亮 + 代码预览）
- 成就管理系统设置

---

## 技术架构

### 技术栈

| 层 | 技术 |
|----|------|
| 后端 | Python 3.11+ / FastAPI / SQLAlchemy 2.0 (异步) / Alembic |
| 学习前端 | React 19 / TypeScript / Vite / TailwindCSS 4 / Monaco Editor / Chart.js / Zustand |
| 管理前端 | React 19 / TypeScript / Vite / Ant Design 6 / Zustand |
| AI 服务 | DeepSeek-V4-Flash（主用）/ Mock 兜底 |
| 数据库 | SQLite + aiosqlite（开发）/ PostgreSQL 16 + asyncpg（生产） |
| 认证 | JWT Bearer Token + Refresh Token / bcrypt |
| 代码执行 | subprocess 直接执行 |

### 系统架构

```
┌──────────────┐  ┌──────────────┐
│ 学习前端      │  │ 管理后台      │
│ React :5173  │  │ React :5174  │
└──────┬───────┘  └──────┬───────┘
       │                 │
       └────────┬────────┘
                │
         ┌──────▼──────┐
         │  FastAPI    │
         │  :8006      │
         └──────┬──────┘
                │
    ┌───────────┼───────────┐
    │           │           │
┌───▼───┐ ┌────▼────┐ ┌───▼───┐
│SQLite │ │ Redis 7│ │Docker │
│  DB   │ │  Cache │ │Sandbox│
└───────┘ └────────┘ └───────┘
```

### 数据模型

```
User ──< Progress ──> Lesson ──< Language
  │       │
  │       └── Submission (lesson_id)
  ├──< UserAchievement ──> Achievement
  ├──< AgentProgress ──> NeuronNode
  └──< CodeSnippet
```

### API 端点（37 个）

| 类别 | 端点示例 |
|------|----------|
| 认证 | POST `/auth/register` `/auth/login` `/auth/refresh` |
| 课程关卡 | GET `/languages` `/languages/{slug}/map` `/lessons/{id}` `/lessons/recommend`，POST `/lessons/{id}/submit` |
| AI 智能体 | POST `/ai/diagnostic` `/ai/tutor` `/ai/review` `/ai/plan` `/ai/classify-error` `/ai/chat`，WS `/ws/ai/chat` |
| 诊断错题知识 | GET `/diagnostic/questions` `/errors` `/progress/knowledge`，POST `/diagnostic/submit` |
| 用户成就排行 | GET `/users/me/stats` `/achievements` `/leaderboard` `/users/me/activity` |
| 代码收藏 | GET/POST `/snippets`，DELETE `/snippets/{id}` |
| 智能体工坊 | GET `/agent/map` `/agent/tracks`，POST `/agent/nodes/{id}/submit` |
| 会员 | POST `/user/upgrade`，GET `/user/membership` |

### 安全设计

- JWT Bearer Token 认证 + Refresh Token 过期校验
- bcrypt 密码哈希
- 被封禁用户登录拦截
- CORS 白名单 + 速率限制 (login/register/submit/AI)
- Docker 沙箱优先 → 直接执行需显式配置
- SQLite PRAGMA foreign_keys = ON + cascade delete
- 异常脱敏输出 + 完整日志
- 并发控制 Semaphore(10) + threading.Lock 沙箱检测
- Windows GBK 编码兼容 (_safe_decode 多编码尝试)

---

## 目录结构

```
Hello World/
├── codequest-api/            # FastAPI 后端
│   ├── app/
│   │   ├── main.py / config.py / database.py
│   │   ├── models/           # 13 个 ORM 模型（user/course/lesson/diagnostic/knowledge/error 等）
│   │   ├── routers/          # 14 个路由模块（含 admin/ 子路由）
│   │   ├── services/         # 13 个业务服务（judge/ai/agent/knowledge/error 等）
│   │   ├── schemas/ / core/ / judge/
│   │   └── judge/            # 评测语言配置 + Docker 沙箱
│   ├── scripts/              # 课程校验脚本
│   └── requirements.txt
│
├── codequest-web/             # 学习前端 (React 19 + TailwindCSS 4 + Monaco + Zustand)
│   └── src/
│       ├── pages/             # 11 个页面（Lobby/CourseMap/Lesson/NeuralMap/Profile 等）
│       ├── components/        # chat/editor/auth/badge/ui/snippets/RadarChart
│       ├── api/ / hooks/ / stores/ / utils/
│
├── codequest-admin-web/       # 管理后台 (React 19 + Ant Design 6 + Zustand)
│   └── src/
│       ├── pages/             # 8 个管理页面
│       └── components/ / api/ / stores/
│
├── codequest-content/
│   ├── lessons/               # 6 语言 × 172 关 JSON
│   ├── agent/                 # 8 主线 × 92 节点 JSON
│   └── ai_prompts.json       # 语言级 AI Prompt
│
└── docs/ / scripts/ / .env.example / docker-compose.yml
```

---

## 亮点与创新

### 1. 14 语言代码沙箱评测

自研评测引擎支持 14 种编程语言的 Docker 沙箱隔离执行。每种语言有独立的 wrapper 模板和编译/运行命令配置。6 级智能匹配策略允许部分通过的答案获得比例分数——不只有 0/100 分。

### 2. 神经元网络解锁机制

智能体工坊的 92 个知识点节点构成有向无环图，每个节点的前置依赖可以是同轨道的 order 编号或其他轨道的节点 slug。前端渲染为分组卡片网格，实时显示 locked → available → in_progress → completed → mastered 状态流转。

### 3. Windows GBK 编码兼容

Windows 下 subprocess 执行 Python 代码时，错误输出为系统编码 (GBK/cp936)，直接使用 `encoding='utf-8'` 会导致 reader thread 崩溃、stderr 丢失。通过二进制模式捕获 + `_safe_decode` 多编码尝试（utf-8 → gbk → cp936 → latin-1）彻底解决了这个跨平台编码问题。

### 4. 全功能零外部依赖前端

学习前端不依赖任何 UI 组件库——所有界面（玻璃拟态卡片、3D 悬浮效果、动画边框、星级评分、热力图、统计图表）均使用 TailwindCSS + 内联 SVG 实现，包体积小、加载快。

### 5. 增量种子数据系统

种子服务支持增量更新——已存在的语言/课时不会被重复插入，新增语言自动追加。前置依赖支持数字（order）和字符串（slug）两种引用方式，按文件逐个翻译为数据库 ID。

---

## 运行方式

```bash
# 后端 (Python 3.11+, port 8006)
cd codequest-api && uvicorn app.main:app --host 0.0.0.0 --port 8006 --reload

# 学习前端 (Node.js 20+, port 5173)
cd codequest-web && npx vite --port 5173 --host

# 管理后台 (port 5174, admin / admin123)
cd codequest-admin-web && npx vite --port 5174 --host
```

---

> Hello World © 2026 — 让编程学习像游戏一样有趣
