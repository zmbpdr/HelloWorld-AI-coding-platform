# CodeQuest — 闯关式 AI 编程学习平台

## 作品简介

CodeQuest 是一个面向编程初学者的**闯关式 AI 编程学习平台**。用户通过编写代码通过测试用例来闯关获得经验值，平台集成 AI 导师进行实时代码错误分析，Docker 沙箱安全执行用户代码。

平台支持**三大学习板块**：14 门编程语言闯关（258 节课时）、智能体工坊（8 条 AI/ML 学习主线，92 个神经元节点）、机器学习实验室（JupyterLab + PyTorch 容器）。

---

## 核心功能

### 编程闯关（14 语言 · 258 关卡）

| 语言 | 课时 |
|------|------|
| Python / JavaScript / Java / C++ | 24 / 20 / 20 / 18 |
| Go / Rust / TypeScript / SQL | 24 / 24 / 20 / 16 |
| Ruby / Swift / Kotlin / PHP | 16 / 16 / 16 / 16 |
| Shell / Lua | 14 / 14 |

- **Monaco 代码编辑器**：支持 14 种语言语法高亮和智能提示
- **智能评测引擎**：6 级匹配策略（精确 → 去空白 → 忽略大小写 → 包含 → 浮点近似 → 去所有空白），支持部分通过按比例得分
- **星级评分**：0-5★，满分且快速执行获 5★
- **评测统计**：每道题的耗时趋势图 + 错误类型分布
- **Docker 沙箱**：no-network, read-only root, 256MB tmpfs 隔离执行
- **学习热力图**：GitHub 风格 90 天活跃度可视化
- **代码收藏夹**：⭐ 一键收藏 + 标签分类 + 全文搜索
- **排行榜**：总榜/月榜/周榜 + 前三奖牌

### AI 导师

- DeepSeek API 主后端 + Ollama 本地模型降级
- 代码错误时自动触发 AI 分析
- 每道题独立对话历史（切换题目不丢失上下文）
- 对话导出为 Markdown

### 智能体工坊（8 主线 · 92 节点）

| 主线 | 节点 | 方向 |
|------|------|------|
| 机器学习 | 8 | 数学基础 → 经典算法 |
| Agent 开发 | 28 | 工具调用 → 多智能体协作 |
| 大模型应用 | 8 | Prompt → RAG 架构 |
| 深度学习 | 12 | 神经网络 → Transformer |
| 自然语言处理 | 10 | 文本预处理 → LLM 微调 |
| 计算机视觉 | 10 | 图像分类 → 目标检测 |
| 强化学习 | 8 | Q-Learning → PPO |
| 综合项目 | 8 | 真实项目实战 |

- 神经元网络地图：前置依赖解锁，按阶段推进
- 能量评级系统：理解力/实现力/优化力/创造力四维评分
- 每个节点独立代码评测

### 机器学习实验室

- JupyterLab + PyTorch 容器化环境
- 5 大主题模板：神经网络、CNN、RNN、NLP、推荐系统
- 内置合成数据生成代码，开箱即用
- 支持学生上传自定义 CSV/JSON 数据集
- 模型权重、可视化图表自动保存可下载

### 管理后台

- 数据仪表盘 + 7 天趋势图
- 用户管理（搜索/封禁/详情）
- 课程管理（14 语言 CRUD + 编辑抽屉）
- 提交审计（按状态/用户名查询 + 异常高亮 + 代码预览）
- 成就管理系统设置

---

## 技术架构

### 技术栈

| 层 | 技术 |
|----|------|
| 后端 | FastAPI + SQLAlchemy 2.0 async + JWT + bcrypt |
| 学习前端 | React 19 + TypeScript + TailwindCSS 4 + Monaco Editor + Zustand |
| 管理前端 | React 19 + TypeScript + Ant Design 6 + Zustand |
| 数据库 | SQLite (开发) / PostgreSQL 16 (生产) |
| 缓存 | Redis 7 |
| AI 服务 | DeepSeek API + Ollama (qwen2.5-coder) 双后端降级 |
| 沙箱 | Docker 容器隔离 (no-network, readonly-root, 256MB, 64pids) |
| ML 实验室 | JupyterLab + PyTorch (Docker 容器) |

### 系统架构

```
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ 学习前端      │  │ 管理后台      │  │ ML 实验室     │
│ React :5173  │  │ React :5174  │  │ Jupyter :8888│
└──────┬───────┘  └──────┬───────┘  └──────────────┘
       │                 │
       └────────┬────────┘
                │
         ┌──────▼──────┐
         │  FastAPI    │
         │  :8006      │
         └──────┬──────┘
                │
    ┌───────────┼───────────┐
    │           │           │
┌───▼───┐ ┌────▼────┐ ┌───▼───┐
│SQLite │ │ Redis 7│ │Docker │
│  DB   │ │  Cache │ │Sandbox│
└───────┘ └────────┘ └───────┘
```

### 数据模型

```
User ──< Progress ──> Lesson ──< Language
  │       │
  │       └── Submission (lesson_id)
  ├──< UserAchievement ──> Achievement
  ├──< AgentProgress ──> NeuronNode
  └──< CodeSnippet
```

### API 端点（25+ 个）

```
POST   /auth/register, /auth/login, /auth/refresh
GET    /languages, /languages/{slug}, /languages/{slug}/map
GET    /lessons/{id}, POST /lessons/{id}/submit
GET    /lessons/{id}/stats, GET /lessons/{id}/hint
POST   /ai/chat, WS /ws/ai/chat
GET    /achievements, /users/me/stats, /users/me/achievements
GET    /users/me/activity, /leaderboard
GET    /snippets, POST /snippets, DELETE /snippets/{id}
GET    /agent/map, /agent/nodes/{id}
POST   /agent/nodes/{id}/submit, /agent/progress, /agent/tracks
```

### 安全设计

- JWT Bearer Token 认证 + Refresh Token 过期校验
- bcrypt 密码哈希
- 被封禁用户登录拦截
- CORS 白名单 + 速率限制 (login/register/submit/AI)
- Docker 沙箱优先 → 直接执行需显式配置
- SQLite PRAGMA foreign_keys = ON + cascade delete
- 异常脱敏输出 + 完整日志
- 并发控制 Semaphore(10) + threading.Lock 沙箱检测
- Windows GBK 编码兼容 (_safe_decode 多编码尝试)

---

## 目录结构

```
闯关/
├── codequest-api/            # FastAPI 后端
│   └── app/
│       ├── models/            # 10+ ORM 模型
│       ├── schemas/           # Pydantic 验证
│       ├── routers/           # 15+ 路由模块
│       ├── services/          # 业务逻辑层
│       ├── core/              # 安全/依赖/中间件
│       └── judge/             # 代码执行引擎
│
├── codequest-web/             # 学习前端 (React + TailwindCSS + Monaco)
│   └── src/
│       ├── pages/             # 9 个页面组件
│       ├── components/        # UI/业务组件
│       ├── api/ / hooks/ / stores/
│
├── codequest-admin-web/       # 管理后台 (React + Ant Design 6)
│   └── src/
│       ├── pages/             # 9 个管理页面
│       ├── components/        # Layout / StatCard / TrendChart
│       └── api/ / stores/
│
├── codequest-content/
│   ├── lessons/               # 14 语言 × 258 课时 JSON
│   └── agent/                 # 8 主线 × 92 节点 JSON
│
├── notebooks/                 # ML 实验模板
├── student-work/              # 学生 Jupyter 工作区
└── outputs/                   # 训练产出
```

---

## 亮点与创新

### 1. 14 语言代码沙箱评测

自研评测引擎支持 14 种编程语言的 Docker 沙箱隔离执行。每种语言有独立的 wrapper 模板和编译/运行命令配置。6 级智能匹配策略允许部分通过的答案获得比例分数——不只有 0/100 分。

### 2. 神经元网络解锁机制

智能体工坊的 92 个知识点节点构成有向无环图，每个节点的前置依赖可以是同轨道的 order 编号或其他轨道的节点 slug。前端渲染为分组卡片网格，实时显示 locked → available → in_progress → completed → mastered 状态流转。

### 3. Windows GBK 编码兼容

Windows 下 subprocess 执行 Python 代码时，错误输出为系统编码 (GBK/cp936)，直接使用 `encoding='utf-8'` 会导致 reader thread 崩溃、stderr 丢失。通过二进制模式捕获 + `_safe_decode` 多编码尝试（utf-8 → gbk → cp936 → latin-1）彻底解决了这个跨平台编码问题。

### 4. 全功能零外部依赖前端

学习前端不依赖任何 UI 组件库——所有界面（玻璃拟态卡片、3D 悬浮效果、动画边框、星级评分、热力图、统计图表）均使用 TailwindCSS + 内联 SVG 实现，包体积小、加载快。

### 5. 增量种子数据系统

种子服务支持增量更新——已存在的语言/课时不会被重复插入，新增语言自动追加。前置依赖支持数字（order）和字符串（slug）两种引用方式，按文件逐个翻译为数据库 ID。

---

## 运行方式

```bash
# 后端 (Python 3.11+, port 8006)
cd codequest-api && uv run uvicorn app.main:app --host 0.0.0.0 --port 8006 --reload

# 学习前端 (Node.js 20+, port 5173)
cd codequest-web && npx vite --port 5173 --host

# 管理后台 (port 5174, admin / admin123)
cd codequest-admin-web && npx vite --port 5174 --host

# ML 实验室 (Docker, port 8888, token=codequest)
docker run -d --name codequest-ml-lab -p 8888:8888 \
  -v ./notebooks:/home/jovyan/templates:ro \
  -v ./student-work:/home/jovyan/student-work \
  -v ./outputs:/home/jovyan/outputs \
  -e JUPYTER_ENABLE_LAB=yes -e JUPYTER_TOKEN=codequest \
  quay.io/jupyter/pytorch-notebook:latest
```

---

> CodeQuest © 2026 — 让编程学习像游戏一样有趣
