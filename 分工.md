# 团队成员任务安排：教师创作工具

## 总体分工概览

| 成员 | 角色定位 | 技术栈 | 核心职责 |
|------|----------|--------|----------|
| A | 后端核心 | Python、FastAPI、SQLAlchemy、Alembic | 图片上传API、题库数据库设计与迁移、Crud API、Seed 逻辑改造、Word/PDF 解析 |
| B | 管理后台前端 | React、TypeScript、Ant Design、markdown-it | Markdown 编辑器组件、工具栏、图片上传前端集成、题库管理页面、文件导入前端 |
| C | 全栈集成 | Python、FastAPI、React、TypeScript | 学生端渲染增强、LessonEditor 替换、批量导入预检查后端、教程-题目关联API、批量导出 |
| D | 测试与质量保障 | Python、pytest、React、TypeScript | 安全测试、功能测试、数据迁移测试、集成测试、端到端验证 |

---

## 第一步：Markdown教程编辑器（P0，1周）

目标：教师可以在管理后台编写图文并茂的教程，支持图片粘贴/拖拽/选择上传，学生端代码块有语法高亮。

### 成员A：后端图片上传API

**用什么：**

Python、FastAPI、UploadFile
python-magic（或手动magic bytes检测）做文件类型校验
uuid生成文件名
Pillow（可选）做图片尺寸/格式二次验证

**做什么：**

1. 新增 POST /api/v1/admin/lessons/upload-image 接口
   - 依赖注入 require_role("editor") 校验管理员权限
   - 接收 UploadFile，限制大小 ≤ 5MB
   - 服务端读取文件头 magic bytes 校验真实类型（jpg/png/gif/webp），不信任浏览器 MIME 类型
   - 禁止 SVG（可携带脚本）
   - 生成 UUID 文件名，按日期分目录存储：static/uploads/{YYYYMMDD}/{uuid}.{ext}
   - 返回 { url: "/uploads/20260806/abc123.png" }

2. 在 main.py 中挂载 static/ 目录为静态文件（StaticFiles），使上传的图片可公开访问

3. 配置文件新增 UPLOAD_DIR 和 MAX_UPLOAD_SIZE 配置项

**产出：**

- app/routers/admin/upload.py（新文件，图片上传路由）
- app/config.py（新增 UPLOAD_DIR、MAX_UPLOAD_SIZE）
- app/main.py（新增静态文件挂载）

**交接给：** B（API接口地址和返回格式）、D（安全测试用例）

---

### 成员B：Markdown编辑器组件

**用什么：**

- React、TypeScript、Ant Design（Button、Tooltip、Space、Upload、message）
- 现有 markdown-it（复用预览渲染）
- highlight.js（代码语法高亮，后续 C 在学生端也会用到）

**做什么：**

1. 编写 MarkdownEditor 组件（src/components/MarkdownEditor.tsx）
   - 左右分栏布局：左侧编辑区（textarea）+右侧实时预览区
   - 工具栏按钮：H1-H6 标题、加粗、斜体、行内代码、代码块（带语言选择）、有序列表、无序列表、链接、图片
   - 点击工具栏按钮在光标位置插入对应 Markdown 语法
   - 预览区使用 markdown-it 实时渲染，添加 highlight.js 代码块语法高亮
   - 支持 GFM 扩展（表格、任务列表）

2. 图片上传集成
   - 工具栏"插入图片"按钮 → 调用 Ant Design Upload 组件选择文件 → 上传到 A 的 API → 返回 URL → 插入 ![](url) 语法
   - 拖拽图片到编辑区 → 自动上传 → 插入 Markdown 语法
   - Ctrl+V 粘贴图片 → 检测剪贴板中的图片数据 → 上传 → 插入 Markdown 语法

3. 在 src/api/admin.ts 中新增 uploadImage(file: File): Promise<{url: string}> 函数

**产出：**

- src/components/MarkdownEditor.tsx（新文件）
- src/api/admin.ts（扩展，新增 uploadImage 函数）

**交接给：** C（组件集成）、D（功能测试）

---

### 成员C：学生端渲染增强 + LessonEditor 替换

**用什么：**

- React、TypeScript
- 现有 markdown-it（复用）
- highlight.js（与 B 保持一致）

**做什么：**

1. 学生端渲染增强（src/utils/markdown.ts）
   - 引入 highlight.js，配置 markdown-it 的 highlight 函数
   - 启用 GFM 扩展（markdown-it 已内置表格支持，确认任务列表等插件）
   - 确保图片路径正确渲染（相对路径 /uploads/... 可正常显示）
   - 引入 highlight.js 的 CSS 主题文件（建议 github 或 atom-one-light）

2. 替换 LessonEditor 中的裸 textarea
   - 在 src/pages/LessonEditor.tsx 中，将 content 字段的 <Input.TextArea> 替换为 B 编写的 MarkdownEditor 组件
   - 确保表单提交时 content 值正确传递 Markdown 文本
   - 编辑现有关卡时，content 初始值正确回填到编辑器

**产出：**

- src/utils/markdown.ts（增强：highlight.js + GFM）
- src/pages/LessonEditor.tsx（修改：替换 textarea 为 MarkdownEditor）
- 新增 highlight.js 依赖（npm install highlight.js）

**交接给：** D（渲染效果验证）

---

### 成员D：图片上传安全测试 + 编辑器功能测试

**用什么：**

- Python、pytest、httpx（后端 API 测试）
- 浏览器手动测试 + 截图（前端功能验证）

**做什么：**

1. 图片上传安全测试（后端）
   - 权限校验：无 token 请求返回 401
   - 文件类型校验：上传 SVG 文件被拒绝
   - 文件类型校验：上传改扩展名的可执行文件（如 shell.jpg 实际为 ELF）被 magic bytes 检测拒绝
   - 文件大小：上传 6MB 图片被拒绝
   - 正常上传：jpg/png/gif/webp 各一张，验证返回 URL 可访问
   - UUID 文件名验证：上传的文件名不是原始文件名

2. 编辑器功能测试（前端）
   - 工具栏每个按钮点击后，textarea 中正确插入 Markdown 语法
   - 实时预览：输入 Markdown 文本后，预览区正确渲染
   - 图片拖拽上传：拖入图片后自动上传并插入语法
   - 图片粘贴上传：Ctrl+V 粘贴截图后自动上传并插入语法
   - 编辑现有关卡：content 内容正确回填并渲染

3. 学生端渲染测试
   - 包含代码块的教程页面，代码块有语法高亮
   - 包含图片的教程页面，图片正常显示
   - 包含表格的教程页面，表格正确渲染

**产出：**

- tests/test_upload_image.py（后端测试文件）
- 测试报告（Markdown，列出通过/失败的用例及截图）

**交接给：** A、B、C（发现的 Bug 列表）

---

## 第二步：题库管理系统（P0，2周）

目标：题库从 JSON 文件迁移到数据库，教师可独立管理题库（增删改查、批量导入导出、与教程关联），系统重启不再覆盖教师修改。

### 成员A：题库数据库设计与后端API

**用什么：**

- Python、SQLAlchemy、Alembic（数据库迁移）
- openpyxl 或 pandas（Excel 读取）
- 现有 seed_service.py（改造）

**做什么：**

1. 数据库设计
   - 新增 questions 表（app/models/question.py）：
     - id、language_id（外键）、title、description、difficulty、test_cases（JSON）、knowledge_tags（JSON）、source（手动/导入）、created_by（admin_id）、created_at、updated_at
   - 新增 lesson_questions 关联表：lesson_id、question_id、order（多对多）
   - 编写 Alembic 迁移脚本

2. 题库 CRUD API（app/routers/admin/questions.py）
   - GET /api/v1/admin/questions — 列表（分页、按语言/知识点/难度筛选）
   - GET /api/v1/admin/questions/{id} — 详情
   - POST /api/v1/admin/questions — 新增
   - PUT /api/v1/admin/questions/{id} — 编辑
   - DELETE /api/v1/admin/questions/{id} — 删除

3. Seed 逻辑改造（app/services/seed_service.py）
   - 将"每次启动覆盖导入"改为"仅首次初始化"
   - 检测逻辑：数据库 questions 表是否已有数据，有则跳过
   - 历史 JSON 文件保留作为初始化数据源和备份

4. 批量导入 API（POST /api/v1/admin/questions/import）
   - 接收 Excel/CSV 文件
   - 不直接入库，先返回预检查报告（具体逻辑由 C 实现，见下方）
   - 确认后调用入库接口，使用事务，失败时整体回滚

5. 批量导出 API（GET /api/v1/admin/questions/export）
   - 导出为 Excel，包含所有字段

**产出：**

- app/models/question.py（新文件，数据模型）
- app/routers/admin/questions.py（新文件，Crud + 导入导出 API）
- Alembic 迁移脚本
- app/services/seed_service.py（改造）

**交接给：** B（API 接口地址和参数格式）、C（批量导入预检查接口对接）、D（数据库迁移测试）

---

### 成员B：题库管理页面

**用什么：**

- React、TypeScript、Ant Design（Table、Form、Modal、Upload、Tag、Select）
- src/api/admin.ts（扩展）

**做什么：**

1. 题库管理页面（src/pages/QuestionManager.tsx）
   - 题目列表（Ant Design Table）：分页、按语言/知识点/难度筛选、关键词搜索
   - 新增/编辑题目（Modal表单）：标题、语言、难度、描述、测试用例（JSON编辑器或结构化表单）、知识点标签
   - 删除题目（确认弹窗）

2. 批量导入页面（src/pages/QuestionImport.tsx）
   - 上传 Excel/CSV 文件
   - 展示预检查结果：错误行号、字段名、错误原因（表格 + 高亮错误行）
   - "确认导入"按钮（仅无错误时可点击）
   - 导入进度提示
   - 模板下载按钮（提供标准 Excel 模板）

3. 在 src/api/admin.ts 中新增：
   - getQuestions(params) — 题目列表
   - getQuestionDetail(id) — 题目详情
   - createQuestion(data) / updateQuestion(id, data) / deleteQuestion(id)
   - importQuestions(file) — 批量导入
   - exportQuestions(params) — 批量导出

4. 教程-题目关联（在 LessonEditor.tsx 中新增）
   - 编辑关卡时，可选择关联已有题库题目
   - 多选组件（Select 多选），按语言/知识点筛选题目

**产出：**

- src/pages/QuestionManager.tsx（新文件）
- src/pages/QuestionImport.tsx（新文件）
- src/api/admin.ts（扩展，新增题库相关 API 函数）
- src/pages/LessonEditor.tsx（修改，新增题目关联）

**交接给：** C（教程-题目关联的 API 对接）、D（功能测试）

---

### 成员C：批量导入预检查后端 + 教程-题目关联 API + 批量导出

**用什么：**

- Python、FastAPI、openpyxl 或 pandas
- 现有 judge_service.py（测试用例格式校验）

**做什么：**

1. 批量导入预检查后端逻辑
   - 解析 Excel/CSV 文件，逐行校验
   - 校验项：
     - 必填字段缺失（title、language_id、difficulty、test_cases）
     - slug 重复（与已有题库对比）
     - 测试用例格式无效（复用 judge_service.py 的测试用例结构校验）
     - 知识点标签不在允许范围内
     - 难度值不在枚举范围内
   - 返回结构化的错误报告：{ errors: [{ row: 3, field: "test_cases", message: "测试用例格式无效" }] }
   - 确认入库接口：接收校验通过的数据，使用数据库事务批量写入，失败整体回滚

2. 教程-题目关联 API
   - POST /api/v1/admin/lessons/{lesson_id}/questions — 关联题目到教程
   - DELETE /api/v1/admin/lessons/{lesson_id}/questions/{question_id} — 取消关联
   - GET /api/v1/admin/lessons/{lesson_id}/questions — 获取教程关联的题目列表

3. 批量导出后端
   - 生成 Excel 文件，包含所有字段
   - 支持按语言/知识点/难度筛选导出

**产出：**

- app/services/import_validator.py（新文件，导入校验逻辑）
- app/routers/admin/questions.py（与 A 协作，预检查 + 确认入库 + 导出接口）
- app/routers/admin/lesson_questions.py（新文件，教程-题目关联 API）

**交接给：** B（预检查接口返回格式，前端对接）、D（导入校验测试）

---

### 成员D：数据迁移测试 + 批量导入测试 + 集成测试

**用什么：**

- Python、pytest、httpx
- SQLite 测试数据库（sqlite+aiosqlite:///test.db）

**做什么：**

1. 数据迁移测试
   - JSON 文件 → 数据库首次导入，数据完整性校验（条数、字段值一致）
   - 重复启动：二次启动不覆盖已有数据
   - 教师新增数据后重启：新增数据保留，不被 JSON 覆盖
   - 边界情况：JSON 文件为空、JSON 格式错误

2. 批量导入测试
   - 正常导入：全部数据正确入库
   - 缺失字段：预检查返回错误行号和字段名
   - 重复 slug：预检查检测到重复
   - 无效测试用例：预检查检测到格式错误
   - 事务回滚：部分数据错误时，全部数据不回库
   - 空文件导入

3. 教程-题目关联测试
   - 关联题目到教程
   - 取消关联
   - 删除教程时关联记录清理
   - 删除题目时关联记录清理

4. 端到端测试
   - 教师登录 → 进入题库管理 → 上传 Excel → 查看预检查报告 → 确认导入 → 题目出现在列表中
   - 教师编辑关卡 → 关联题库题目 → 保存 → 学生端查看关卡可看到关联题目

**产出：**

- tests/test_question_migration.py（数据迁移测试）
- tests/test_question_import.py（批量导入测试）
- tests/test_lesson_question.py（教程-题目关联测试）
- 测试报告（Markdown，列出所有用例通过/失败情况）

**交接给：** A、B、C（发现的 Bug 列表）

---

## 第三步：PDF/Word文件导入（P1，按需启动，2-3周）

**触发条件**：前两步完成后，验证教师确实有文件导入需求。如果教师愿意使用 Markdown 编辑器，这一步可简化或不做。

**目标**：教师上传 PDF/Word 文件，系统自动解析为教程内容填入编辑器。

### 成员A：Word/PDF解析后端

**用什么：**

- Python、python-docx（Word解析）、PyMuPDF（fitz，PDF解析）
- 现有图片上传API（复用）

**做什么：**

1. Word 解析（POST /api/v1/admin/lessons/parse-word）
   - 接收 Word 文件，使用 python-docx 逐段落解析
   - 段落 → Markdown 段落（保留加粗、斜体、标题层级）
   - 内嵌图片 → 提取 → 调用图片上传 API → 替换为 Markdown 图片语法
   - 表格 → Markdown 表格
   - 返回 Markdown 文本供前端填入编辑器

2. PDF 解析（POST /api/v1/admin/lessons/parse-pdf）
   - 接收 PDF 文件，使用 PyMuPDF 提取文本和图片
   - 文本提取（含基本排版保留）
   - 图片提取 → 上传 → 替换为 Markdown 图片语法
   - 保留原始 PDF 文件供下载（解析结果不保证 100% 还原）
   - 返回 Markdown 文本 + 原始文件下载链接

**产出：**

- app/routers/admin/file_import.py（新文件，Word/PDF 解析 API）
- requirements.txt（新增 python-docx、PyMuPDF）

**交接给：** B（解析结果格式，前端对接）

---

### 成员B：文件导入前端

**用什么：**

- React、TypeScript、Ant Design（Upload、Button、Spin）

**做什么：**

1. 在 MarkdownEditor 组件中新增"导入文件"按钮
   - 点击弹出文件选择器（支持 .docx、.pdf、.md）
   - 上传文件 → 调用对应解析 API → 获取 Markdown 文本
   - 将解析结果填入编辑器
   - 加载状态提示

2. Markdown 文件导入
   - 直接读取文件内容填入编辑器
   - 处理图片路径（相对路径 → 上传后替换为绝对 URL）

3. 解析结果预览
   - 解析完成后，在编辑器中展示结果
   - 提示用户"解析结果可能需要手动调整"（PDF 尤其）

**产出：**

- src/components/MarkdownEditor.tsx（修改，新增文件导入功能）
- src/api/admin.ts（新增 parseWord、parsePdf 函数）

**交接给：** D（跨格式测试）

---

### 成员C：第三步无独立任务

C 在第三步可投入其他方向（如性能优化、Bug 修复），或协助 D 做跨格式测试。

---

### 成员D：跨格式导入测试

**用什么：**

- Python、pytest、httpx

**做什么：**

1. Word 导入测试
   - 纯文本 Word → 解析结果正确
   - 含图片 Word → 图片正确提取并替换为 Markdown 语法
   - 含表格 Word → 表格正确转为 Markdown 表格
   - 含代码块 Word → 代码块正确处理
   - 复杂排版（分栏、文本框）→ 验证解析结果可接受

2. PDF 导入测试
   - 纯文本 PDF → 文本提取正确
   - 含代码块 PDF → 缩进和格式保留情况
   - 含图片 PDF → 图片提取正确
   - 原始文件下载链接可用

3. Markdown 文件导入测试
   - 含相对路径图片的 MD 文件 → 图片路径处理正确

**产出：**

- tests/test_file_import.py（文件导入测试）
- 测试报告

---

## 成员间交接关系

### 第一步（1周）：

- A → B：图片上传 API 接口地址和返回格式
- B → C：MarkdownEditor 组件 Props 接口和使用方式
- C → D：修改后的文件列表，供测试验证
- D → A/B/C：测试发现的 Bug 列表

### 第二步（2周）：

- A → B：题库 CRUD API 接口地址和参数格式
- A → C：预检查接口对接（确认入库接口）
- C → B：预检查接口返回格式，前端对接
- B → C：教程-题目关联的 API 对接格式
- C → A：关联 API 需求对接
- D → A/B/C：测试发现的 Bug 列表

### 第三步（按需启动）：

- A → B：Word/PDF 解析 API 接口和返回格式
- B → D：前端文件导入功能，供测试验证

---

## 时间线总览

| 周次 | A | B | C | D |
|------|---|---|---|---|
| 第1周 | 图片上传 API + 静态文件挂载 | Markdown 编辑器组件 + 图片上传集成 | 学生端渲染增强 + LessonEditor 替换 | 图片上传安全测试 + 编辑器功能测试 |
| 第2周 | 题库 DB 设计 + 迁移 + Crud API | 题库管理页面 | 批量导入预检查后端 | 数据迁移测试 |
| 第3周 | Seed 逻辑改造 + 批量导入 API | 批量导入页面 + 教程-题目关联前端 | 教程-题目关联 API + 批量导出 | 批量导入测试 + 集成测试 |
| 第4-6周（按需） | Word/PDF 解析后端 | 文件导入前端 | 协助测试 | 跨格式导入测试 |

---

## 关键约束

1. 第一步必须在 1 周内完成并演示，这是验证方向的里程碑
2. 第二步的数据迁移必须在测试通过后才能上线，防止覆盖教师已有数据
3. 所有图片上传接口必须先通过 D 的安全测试，才能合并到主分支
4. 批量导入必须先通过预检查，确认无误后才能入库，不可跳过
5. 第三步为 P1 按需启动，前两步完成后根据教师反馈决定是否投入